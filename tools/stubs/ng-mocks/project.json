{
  "name": "ng-mocks",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "root": "tools/stubs/ng-mocks",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "$comment": "@nx/eslint/plugin infers a target named `eslint:lint`, which `nx affected -t lint` does NOT match \u2014 so neither the gate nor CI ever linted this project. An `nx.targets` block in package.json is not read for a package.json-inferred project, so the target is declared here instead."
    }
  }
}
