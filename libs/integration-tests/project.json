{
  "name": "integration-tests",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/integration-tests/src",
  "projectType": "library",
  "tags": ["type:integration-test"],
  "$tagsComment": "`scope:shared` was here alongside `type:integration-test`. Nx permits a dependency when ANY tag matches, so `scope:shared` let any feature library import a harness that issues `DELETE /nuxeo/api/v1/path/...` and `Document.Trash`, while `type:integration-test` appeared in no rule in `eslint.config.mjs` and governed nothing. The single tag is now the only one, and `eslint.config.mjs` names it as a source but never as a permitted target — so this library may read the platform it drives and nothing may depend on it. Verified by importing it from a feature library and watching lint reject the edge.",
  "// targets": "to see all targets run: nx show project integration-tests --web",
  "targets": {
    "integration": {
      "executor": "nx:run-commands",
      "$comment": "Named `integration`, not `test`, so no `-t test` invocation can reach it. These specs require a live Nuxeo; CI runners have none, so `nx affected -t test` and `nx run-many -t test` would fail every one of them on a precondition. An exclusion list in each workflow would work only for as long as everyone remembers to extend it — the name cannot be forgotten.",
      "$preflightComment": "Preflight first, so an absent or empty Nuxeo reports as a precondition rather than as sixty-five product failures. Same shape as nuxeo-ui-e2e's `e2e`. Note that `nx` normalises a failed command to exit 1, so the preflight's exit 2 does NOT survive `nx run integration-tests:integration` — `npm run beta:integration` chains the same two commands with `&&` and does preserve it. That is why both exist, exactly as `beta:e2e` exists beside `nuxeo-ui-e2e:e2e`.",
      "$cacheComment": "Deliberately NOT cached: the result depends on a live Nuxeo's contents, and a cached green would assert something about a server state that has since changed.",
      "cache": false,
      "options": {
        "cwd": "{workspaceRoot}",
        "commands": [
          "npx tsx libs/integration-tests/src/preflight-cli.ts",
          "npx vitest run --config libs/integration-tests/vitest.config.mts"
        ],
        "parallel": false
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "$comment": "`preflight-cli.ts` is neither a spec nor part of any bundle, so `spec-typecheck.mjs` does not reach it and there is no `build` target that would. Without this target the one file in the repository whose job is to exit with the right code would be the one file nothing type-checks.",
      "cache": true,
      "inputs": ["default", "^default"],
      "options": {
        "cwd": "{workspaceRoot}",
        "command": "tsc -p libs/integration-tests/tsconfig.lib.json --noEmit"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  },
  "$noTestTarget": "There is deliberately no `test` target — see the `integration` target above. The second consequence is the coverage ratchet: `scripts/beta-harness/coverage-gate.mjs` discovers projects by the presence of `targets.test`, so this project is no longer in its population at all. That is correct rather than an omission to be patched with an OUT_OF_SCOPE entry: these specs run against a live server, never in the coverage run, and a project that is never discovered can never be measured, so an entry excusing it from the 90% bar would be config nothing reads. `nuxeo-ui-e2e` is in the same position and has no entry either."
}
