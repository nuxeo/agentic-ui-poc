import globalAxios from 'axios';

/* tslint:disable */
/* eslint-disable */
/**
 * hxpr-app API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2.1.479
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
// Some imports not used depending on template conditions
// @ts-ignore
const BASE_PATH = "http://localhost:8080".replace(/\/+$/, "");
/**
 *
 * @export
 */
const COLLECTION_FORMATS = {
    csv: ",",
    ssv: " ",
    tsv: "\t",
    pipes: "|"
};
/**
 *
 * @export
 * @class BaseAPI
 */
const DEFAULT_READ_TIMEOUT_MS = 120000;
const LONG_POLL_TIMEOUT_MARGIN_MS = 60000;
function deriveTimeout(url) {
    if (!url)
        return DEFAULT_READ_TIMEOUT_MS;
    const match = url.match(/[?&]timeout=(\d+)/);
    if (match) {
        return parseInt(match[1], 10) * 1000 + LONG_POLL_TIMEOUT_MARGIN_MS;
    }
    return DEFAULT_READ_TIMEOUT_MS;
}
class BaseAPI {
    constructor(configuration, basePath = BASE_PATH, axios = globalAxios) {
        this.basePath = basePath;
        this.axios = axios;
        if (configuration) {
            this.configuration = configuration;
            this.basePath = configuration.basePath || this.basePath;
        }
        // Interceptor added as we do not want encoded '/' characters or repeated '/' through the URL
        axios.interceptors.request.use((req) => {
            if (req.url) {
                // replace encoded '/' characters with literal '/' characters
                req.url = req.url.replace(/%2F/g, "/");
                // replace repeated '/' characters in the middle of the URL with a single '/', skipping 'http(s)://'
                req.url = req.url.replace(/(?<!https?:)\/\/+/g, "/");
            }
            if (req.timeout === undefined) {
                req.timeout = deriveTimeout(req.url);
            }
            return req;
        });
    }
}
;
/**
 *
 * @export
 * @class RequiredError
 * @extends {Error}
 */
class RequiredError extends Error {
    constructor(field, msg) {
        super(msg);
        this.field = field;
        this.name = "RequiredError";
    }
}

export { BASE_PATH, BaseAPI, COLLECTION_FORMATS, RequiredError, deriveTimeout };
//# sourceMappingURL=base.js.map
