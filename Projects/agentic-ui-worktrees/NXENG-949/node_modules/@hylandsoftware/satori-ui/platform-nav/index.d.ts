import * as i0 from '@angular/core';
import { PipeTransform } from '@angular/core';

/**
 * The base interface for navigation items.
 */
interface SatBaseNavigationItem {
    /**
     * The label to display for the navigation item.
     * @required
     */
    label: string;
    /**
     * The path to navigate to when the item is clicked.
     * @required
     */
    path: string;
    /**
     * Determines whether the list item should be rendered in an active/selected state.
     */
    active?: boolean;
}
/**
 * This interface extends `SatBaseNavigationItem`. This is used for navigation items that include an icon.
 */
interface SatNavigationItemWithIcon extends SatBaseNavigationItem {
    /**
     * The icon to display for the navigation item.
     * @required
     */
    icon: string;
}
/**
 * This interface extends `SatBaseNavigationItem`. This is used for navigation items that include an image.
 */
interface SatNavigationItemWithImage extends SatBaseNavigationItem {
    /**
     * The image to display for the navigation item.
     * @required
     */
    image: string;
}
/**
 * The structure of a navigation item used within the platform navigation. It is an intersection of the `SatBaseNavigationItem` or `SatNavigationItemWithIcon` or `SatNavigationItemWithImage` interface. It includes properties for defining the label, path, icon or image, and active state of a navigation item.
 */
type SatNavigationItem = SatBaseNavigationItem | SatNavigationItemWithIcon | SatNavigationItemWithImage;

/**
 * The `SatPlatformNavAvatar` displays an avatar in the platform navigation.
 * It supports rendering an icon, image, or initials based on the provided navigation item.
 *
 * - If the navigation item has an `icon`, it renders a Material icon.
 * - If the navigation item has an `image`, it renders the image inside a custom app icon component.
 * - Otherwise, it renders the initials derived from the navigation item's label.
 *
 * @deprecated This component supports a deprecated usage pattern where content is projected via <ng-content>.
 *
 * Prefer passing a `SatNavigationItem` via the `item` input for avatar rendering.
 */
declare class SatPlatformNavAvatar {
    /**
     * The navigation item associated with this avatar.
     */
    readonly item: i0.InputSignal<SatNavigationItem | undefined>;
    /** @internal */
    readonly icon: i0.Signal<string | null>;
    /** @internal */
    readonly image: i0.Signal<string | null>;
    private itemIsSatNavigationItemWithIcon;
    private itemIsSatNavigationItemWithImage;
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavAvatar, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SatPlatformNavAvatar, "sat-platform-nav-avatar", never, { "item": { "alias": "item"; "required": false; "isSignal": true; }; }, {}, never, ["*"], true, never>;
}

/**
 * This service manages the state of the Platform Nav sidebar.
 * It provides methods to toggle the collapsed state.
 */
declare class SatPlatformNavState {
    private readonly _collapsed;
    /**
     * @internal
     * Whether the sidebar is currently collapsed.
     * You can use this in templates or components to update the UI.
     */
    readonly collapsed: i0.Signal<boolean>;
    /**
     * Call this to toggle the sidebar between collapsed and expanded.
     */
    toggleCollapsed(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavState, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<SatPlatformNavState>;
}

/**
 * This component defines the structural layout for vertical navigation-based applications. It enhances accessibility by supporting a "Skip to Content" feature and allows for flexible content projection. This enables developers to inject key layout elements—such as navigation sidebars and main content areas—while maintaining a clean and organized layout hierarchy.
 *
 * Key features:
 * - Displays a `sat-skip-to-content` element to allow users to quickly skip to the main content for better accessibility.
 * - Projects a `sat-platform-nav` element, typically used for the sidebar or navigation menu.
 * - Projects a `sat-platform-nav-main-content` element, typically used for the application’s main content area.
 * - Defines the main content container with `id="main-content"`, which serves as the target for skip-to-content navigation.
 *
 * @example
 * <sat-platform-nav-container>
 *   <sat-platform-nav>
 *     …
 *   </sat-platform-nav>
 *   <sat-platform-nav-main-content></sat-platform-nav-main-content>
 * </sat-platform-nav-container>
 *
 */
declare class SatPlatformNavContainer {
    /** @internal */
    readonly service: SatPlatformNavState;
    /** @internal */
    closePlatformNav(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavContainer, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SatPlatformNavContainer, "sat-platform-nav-container", never, {}, {}, never, ["sat-platform-nav", "sat-platform-nav-main-content"], true, never>;
}

/**
 * This component is used to display a switcher which allows the user to switch between different environments within the Platform Nav. It allows users to select their current working environment easily.
 */
declare class SatPlatformNavEnvironmentSwitcher {
    /**
     * The currently selected environment.
     * This should match the `value` property of one of the provided environment objects.
     */
    readonly value: i0.InputSignal<string>;
    /**
     * The available environments.
     * This input is required and should be provided with an array of environment objects.
     */
    readonly environments: i0.InputSignal<SatPlatformNavEnvironmentType[]>;
    /**
     * Denotes whether the switcher is disabled.
     */
    readonly disabled: i0.InputSignalWithTransform<boolean, unknown>;
    /**
     * An event emitted when the environment is changed.
     * The value emitted is the currently selected environment.
     * @type SatPlatformNavEnvironmentType
     */
    readonly environmentChange: i0.OutputEmitterRef<SatPlatformNavEnvironmentType>;
    /** @internal */
    readonly satPlatformNavStateService: SatPlatformNavState;
    /**
     * @internal User selection takes precedence over the initial selection.
     */
    readonly selectedEnv: i0.WritableSignal<SatPlatformNavEnvironmentType | undefined>;
    /**
     * @internal
     * Handle option selection
     */
    onSelect(environment: SatPlatformNavEnvironmentType): void;
    private _propagateChanges;
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavEnvironmentSwitcher, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SatPlatformNavEnvironmentSwitcher, "sat-platform-nav-switcher", never, { "value": { "alias": "value"; "required": true; "isSignal": true; }; "environments": { "alias": "environments"; "required": true; "isSignal": true; }; "disabled": { "alias": "disabled"; "required": false; "isSignal": true; }; }, { "environmentChange": "environmentChange"; }, never, never, true, never>;
}

/**
 * This element represents an interactive item within the `sat-platform-nav` navigation panel. It supports icons, dynamic avatar generation, active state styling, and responsive behavior based on the collapsed state of the sidebar.
 *
 * The `SatPlatformNavListItem` is designed to display a single item in the platform navigation.
 *
 * Key features:
 * - Accepts a projected mat-icon for visual representation of the item.
 * - Automatically generates a fallback avatar (initials-based) if no icon is present, using the SatPlatformNavListItemInitialsPipe applied to the item’s label.
 * - Hides the text label when the navigation panel is collapsed, using SatPlatformNavService.
 * - Allows keyboard accessibility supporting navigation via keyboard.
 *
 * @deprecated This component supports a deprecated usage pattern where content is projected for displaying the icon or avatar.
 * The use of mat-icon is deprecated and will be removed in future versions. Prefer using `<sat-platform-nav-avatar>` with the `item` input for avatar rendering.
 *
 * Deprecated usage: Icon render via mat-icon is deprecated.
 */
declare class SatPlatformNavListItem {
    /** The URL to navigate to when the item is clicked. */
    readonly url: i0.InputSignal<string | undefined>;
    /**
     * Determines whether the list item should be rendered in an active/selected state.
     */
    readonly active: i0.InputSignal<boolean>;
    /**
     * @internal
     * The text content of the label, used for tooltip and fallback initials.
     */
    readonly labelText: i0.WritableSignal<string | undefined>;
    /** @internal */
    readonly service: SatPlatformNavState;
    private readonly tooltipService;
    private readonly tooltip;
    private readonly tooltipDelay;
    private tooltipTimer;
    /** @internal */
    onMouseEnter(): void;
    /** @internal */
    onMouseLeave(): void;
    /** @internal */
    labelChanged(event: MutationRecord[]): void;
    private hidePreviousTooltip;
    private showCurrentTooltip;
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavListItem, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SatPlatformNavListItem, "sat-platform-nav-list-item", never, { "url": { "alias": "url"; "required": false; "isSignal": true; }; "active": { "alias": "active"; "required": false; "isSignal": true; }; }, {}, never, ["mat-icon, sat-platform-nav-avatar", "*"], true, never>;
}

/**
 * This component acts as a content projection outlet for the main area of the layout when using the platform navigation. It is intended to be placed adjacent to the vertical sidebar and wraps whatever content you insert within it using ng-content.
 * It serves as a flexible container for your app’s primary page content while structurally aligning with the sidebar layout (SatPlatformNavContainer).
 *
 * Key features:
 * - Supports custom content projection via `ng-content`.
 * - Integrates seamlessly into layouts using `SatPlatformNav`.
 *
 */
declare class SatPlatformNavMainContent {
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavMainContent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SatPlatformNavMainContent, "sat-platform-nav-main-content", never, {}, {}, never, ["*"], true, never>;
}

/**
 * This directive is applied to an Angular Material mat-menu to define a custom-styled user menu that appears when the user profile is clicked.
 *
 * Key features:
 * - Defines the dropdown menu that appears when the profile is clicked.
 * - Enhances mat-menu to match the Platform Nav’s user interface.
 * - Supports standard Angular Material menu behavior.
 * - This directive can apply specific styles, positioning, or behavior consistent with the platform’s design.
 */
declare class SatPlatformNavUserMenu {
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavUserMenu, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<SatPlatformNavUserMenu, "[satPlatformNavUserMenu]", ["satPlatformNavUserMenu"], {}, {}, never, never, true, never>;
}

/**
 * This component displays a clickable user identity block, such as a name along with an avatar that can have initials or user profile image in the platform sidebar’s user section. It also has a responsive behavior based on the collapsed state of the sidebar.
 *
 * Key features:
 * - Typically used to show user info like names, emails, or avatar initials.
 * - Styled for sidebar placement within sat-platform-nav-user.
 */
declare class SatPlatformNavUserProfile {
    /** @internal */
    readonly service: SatPlatformNavState;
    /**
     * @internal
     * The text content of the profile name, used for tooltip and fallback initials.
     */
    readonly profileNameText: i0.WritableSignal<string | undefined>;
    /** @internal */
    profileNameChanged(event: MutationRecord[]): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavUserProfile, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SatPlatformNavUserProfile, "sat-platform-nav-user-profile", never, {}, {}, never, ["mat-icon, img", "*"], true, never>;
}

/**
 * This serves as a container for user-related UI elements within the navigation. It wraps the `SatPlatformNavUserProfile` and provides a slot (`satPlatformNavUserMenu`) for displaying a user menu, typically with dynamic items such as languages or logout options. This component ensures consistent layout and behavior for the user section in the navigation.
 *
 * Key features:
 * - Acts as the container for the user interaction section in the sidebar.
 * - Typically positioned at the bottom of the nav, separating navigation from account-related actions.
 * - Can act as a menu trigger using `[matMenuTriggerFor]`.
 *
 * @example
 * <sat-platform-nav-user>
 *   <button satPlatformNavUserProfile [matMenuTriggerFor]="userMenu">
 *     Lorem Ipsum
 *   </button>
 *   <mat-menu #userMenu="matMenu" satPlatformNavUserMenu>
 *     <button mat-menu-item>
 *       …
 *     </button>
 *   </mat-menu>
 * </sat-platform-nav-user>
 */
declare class SatPlatformNavUser {
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavUser, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SatPlatformNavUser, "sat-platform-nav-user", never, {}, {}, never, ["sat-platform-nav-user-profile, button[satPlatformNavUserProfile]", "[satPlatformNavUserMenu]"], true, never>;
}

/**
 * This component renders the vertical navigation menu, used as a sidebar. It supports static and dynamic nav items. Designed to work inside sat-platform-nav-container, it helps create a well-structured layout for navigation items and an action button for accessing user profile details. It includes control for toggling the collapsed state of the navigation panel, a title that conditionally renders based on the state.
 *
 * Key features:
 * - Dynamically toggles between expanded and collapsed states when user clicks on the h_glyph icon.
 * - Displays the "Content Innovation Cloud" title only when the panel is expanded.
 * - Includes an action button intended to open a popup displaying user profile details.
 */
declare class SatPlatformNav {
    /**
     * @internal
     */
    readonly service: SatPlatformNavState;
    /**
     * @internal
     */
    private readonly tooltipService;
    /**
     * Activates the neutral look of the platform navigation dedicated to administration context.
     */
    readonly neutralTheme: i0.InputSignal<boolean>;
    /**
     * Clears the active tooltip on mouse leave.
     * Resets the tooltipShownFirst flag, hides and disables the current activated tooltip.
     * @internal
     */
    onMouseLeave(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNav, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SatPlatformNav, "sat-platform-nav", never, { "neutralTheme": { "alias": "neutralTheme"; "required": false; "isSignal": true; }; }, {}, never, ["sat-platform-nav-switcher", "*", "sat-platform-nav-user"], true, never>;
}

/**
 * This pipe transforms a name or label into a set of uppercase initials, typically used to display fallback text inside avatar components. It helps maintain a consistent user experience when profile images are not available.
 *
 * > **ℹ️ Note -**
 * > We strongly recommend using the `satPlatformNavInitials` pipe to generate initials for avatars.
 * > This ensures consistent formatting and visual alignment across the navigation item and user profile areas.
 *
 * ```html
 * <sat-platform-nav-avatar>
 *   {{ user.name | satPlatformNavInitials }}
 * </sat-platform-nav-avatar>
 * ```
 *
 * **How It Works**
 *
 * - Extracts the first letter of each word (default: up to 2 words).
 * - For single-word names, returns the first two characters.
 * - Trims whitespace and safely handles empty or invalid strings.
 *
 * **Example Outputs**
 *
 * | Input          | Output |
 * | -------------- | ------ |
 * | John Doe       | JD     |
 * | Mary Ann Smith | MA     |
 * | " "            | ""     |
 *
 * **Optional Parameter**
 *
 * You can pass an optional maxWords parameter to control how many words are considered -
 *
 * ```html
 * <sat-platform-nav-avatar>
 *   {{ 'John David Smith' | satPlatformNavInitials: 1 }} <!-- Output: J -->
 * </sat-platform-nav-avatar>
 * ```
 */
declare class SatPlatformNavInitials implements PipeTransform {
    private readonly _maxWords;
    /**
     * Transforms a name string into initials by taking the first letter
     * of each word (up to the first two words).
     *
     * @param value The name to transform into initials
     * @param maxWords Optional parameter to specify maximum number of words to use (default: 2)
     * @returns The generated initials as uppercase letters
     */
    transform(value: string, maxWords?: number): string;
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavInitials, never>;
    static ɵpipe: i0.ɵɵPipeDeclaration<SatPlatformNavInitials, "satPlatformNavInitials", true>;
}

declare class SatPlatformNavModule {
    static ɵfac: i0.ɵɵFactoryDeclaration<SatPlatformNavModule, never>;
    static ɵmod: i0.ɵɵNgModuleDeclaration<SatPlatformNavModule, never, [typeof SatPlatformNav, typeof SatPlatformNavContainer, typeof SatPlatformNavMainContent, typeof SatPlatformNavListItem, typeof SatPlatformNavUserProfile, typeof SatPlatformNavUser, typeof SatPlatformNavAvatar, typeof SatPlatformNavEnvironmentSwitcher, typeof SatPlatformNavInitials, typeof SatPlatformNavUserMenu], [typeof SatPlatformNav, typeof SatPlatformNavContainer, typeof SatPlatformNavMainContent, typeof SatPlatformNavListItem, typeof SatPlatformNavUserProfile, typeof SatPlatformNavUser, typeof SatPlatformNavAvatar, typeof SatPlatformNavEnvironmentSwitcher, typeof SatPlatformNavInitials, typeof SatPlatformNavUserMenu]>;
    static ɵinj: i0.ɵɵInjectorDeclaration<SatPlatformNavModule>;
}

/**
 * This type defines the structure of an environment used in the environment switcher component. It includes properties for the environment’s name, value, and whether the environment should be disabled.
 */
interface SatPlatformNavEnvironmentType {
    /**
     * The value associated with the environment.
     * @required
     */
    value: string;
    /**
     * The name of the environment.
     * @required
     */
    label: string;
    /**
     * Indicates whether the environment option is disabled.
     */
    disabled?: boolean;
}

export { SatPlatformNav, SatPlatformNavAvatar, SatPlatformNavAvatar as SatPlatformNavAvatarComponent, SatPlatformNav as SatPlatformNavComponent, SatPlatformNavContainer, SatPlatformNavContainer as SatPlatformNavContainerComponent, SatPlatformNavEnvironmentSwitcher, SatPlatformNavEnvironmentSwitcher as SatPlatformNavEnvironmentSwitcherComponent, SatPlatformNavInitials, SatPlatformNavInitials as SatPlatformNavInitialsPipe, SatPlatformNavListItem, SatPlatformNavListItem as SatPlatformNavListItemComponent, SatPlatformNavMainContent, SatPlatformNavMainContent as SatPlatformNavMainContentComponent, SatPlatformNavModule, SatPlatformNavState, SatPlatformNavState as SatPlatformNavStateService, SatPlatformNavUser, SatPlatformNavUser as SatPlatformNavUserComponent, SatPlatformNavUserMenu, SatPlatformNavUserMenu as SatPlatformNavUserMenuDirective, SatPlatformNavUserProfile, SatPlatformNavUserProfile as SatPlatformNavUserProfileComponent };
export type { SatBaseNavigationItem, SatNavigationItem, SatNavigationItemWithIcon, SatNavigationItemWithImage, SatPlatformNavEnvironmentType };
