import * as i0 from '@angular/core';
import { InjectionToken, AfterContentInit, EventEmitter, OnDestroy, ElementRef, OnChanges, SimpleChanges } from '@angular/core';
import * as i1 from '@angular/material/core';
import { DateAdapter } from '@angular/material/core';
import { Subject } from 'rxjs';
import { ControlValueAccessor, Validator, AbstractControl, ValidationErrors } from '@angular/forms';
import { MatDatepickerIntl } from '@angular/material/datepicker';
import * as i10 from '@angular/common';
import * as i11 from '@angular/material/button';
import * as i12 from '@angular/material/dialog';
import * as i13 from '@angular/material/icon';
import * as i14 from '@angular/cdk/overlay';
import * as i15 from '@angular/cdk/a11y';

declare class NativeDatetimeModule {
    static ɵfac: i0.ɵɵFactoryDeclaration<NativeDatetimeModule, never>;
    static ɵmod: i0.ɵɵNgModuleDeclaration<NativeDatetimeModule, never, [typeof i1.NativeDateModule], never>;
    static ɵinj: i0.ɵɵInjectorDeclaration<NativeDatetimeModule>;
}
declare class MatNativeDatetimeModule {
    static ɵfac: i0.ɵɵFactoryDeclaration<MatNativeDatetimeModule, never>;
    static ɵmod: i0.ɵɵNgModuleDeclaration<MatNativeDatetimeModule, never, [typeof NativeDatetimeModule, typeof i1.MatNativeDateModule], never>;
    static ɵinj: i0.ɵɵInjectorDeclaration<MatNativeDatetimeModule>;
}

declare abstract class DatetimeAdapter<D> extends DateAdapter<D> {
    protected _delegate: DateAdapter<D>;
    constructor(_delegate: DateAdapter<D>);
    abstract getHour(date: D): number;
    abstract getMinute(date: D): number;
    abstract getFirstDateOfMonth(date: D): D;
    abstract isInNextMonth(startDate: D, endDate: D): boolean;
    abstract getHourNames(): string[];
    abstract getMinuteNames(): string[];
    abstract addCalendarHours(date: D, months: number): D;
    abstract addCalendarMinutes(date: D, months: number): D;
    abstract createDatetime(year: number, month: number, date: number, hour: number, minute: number): D;
    getValidDateOrNull(obj: any): D | null;
    compareDatetime(first: D, second: D, respectMinutePart?: boolean): number;
    sameDatetime(first: D | null, second: D | null): boolean;
    sameYear(first: D, second: D): boolean;
    sameDay(first: D, second: D): boolean;
    sameHour(first: D, second: D): boolean;
    sameMinute(first: D, second: D): boolean;
    sameMonthAndYear(first: D | null, second: D | null): boolean;
    deserialize(value: any): D | null;
    clone(date: D): D;
    addCalendarYears(date: D, years: number): D;
    addCalendarMonths(date: D, months: number): D;
    addCalendarDays(date: D, days: number): D;
    getYear(date: D): number;
    getMonth(date: D): number;
    getDate(date: D): number;
    getDayOfWeek(date: D): number;
    getMonthNames(style: any): string[];
    getDateNames(): string[];
    getDayOfWeekNames(style: any): string[];
    getYearName(date: D): string;
    getFirstDayOfWeek(): number;
    getNumDaysInMonth(date: D): number;
    createDate(year: number, month: number, date: number): D;
    today(): D;
    parse(value: any, parseFormat: any): D | null;
    format(date: D, displayFormat: any): string;
    toIso8601(date: D): string;
    isDateInstance(obj: any): boolean;
    isValid(date: D): boolean;
    invalid(): D;
    clampDate(date: D, min?: D | null, max?: D | null): D;
}

interface MatDatetimeFormats {
    parse: {
        dateInput?: any;
        monthInput?: any;
        timeInput?: any;
        datetimeInput?: any;
    };
    display: {
        dateInput: any;
        monthInput: any;
        timeInput: any;
        datetimeInput: any;
        monthYearLabel: any;
        dateA11yLabel: any;
        monthYearA11yLabel: any;
        popupHeaderDateLabel: any;
    };
}
declare const MAT_DATETIME_FORMATS: InjectionToken<MatDatetimeFormats>;

declare class NativeDatetimeAdapter extends DatetimeAdapter<Date> {
    constructor();
    clone(date: Date): Date;
    getHour(date: Date): number;
    getMinute(date: Date): number;
    isInNextMonth(startDate: Date, endDate: Date): boolean;
    createDatetime(year: number, month: number, date: number, hour: number, minute: number): Date;
    getFirstDateOfMonth(date: Date): Date;
    getHourNames(): string[];
    getMinuteNames(): string[];
    addCalendarYears(date: Date, years: number): Date;
    addCalendarMonths(date: Date, months: number): Date;
    addCalendarDays(date: Date, days: number): Date;
    addCalendarHours(date: Date, hours: number): Date;
    addCalendarMinutes(date: Date, minutes: number): Date;
    toIso8601(date: Date): string;
    private getDateInNextMonth;
    /**
     * Strip out unicode LTR and RTL characters. Edge and IE insert these into formatted dates while
     * other browsers do not. We remove them to make output consistent and because they interfere with
     * date parsing.
     * @param str The string to strip direction characters from.
     * @returns The stripped string.
     */
    private _stripDirectionalityCharacters;
    /**
     * Pads a number to make it two digits.
     * @param n The number to pad.
     * @returns The padded number.
     */
    private _2digit;
    /** Creates a date but allows the month and date to overflow. */
    private _createDateWithOverflow;
    static ɵfac: i0.ɵɵFactoryDeclaration<NativeDatetimeAdapter, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<NativeDatetimeAdapter>;
}

declare const MAT_NATIVE_DATETIME_FORMATS: MatDatetimeFormats;

declare enum MatDatetimepickerFilterType {
    DATE = 0,
    HOUR = 1,
    MINUTE = 2
}

declare const CLOCK_RADIUS = 50;
declare const CLOCK_INNER_RADIUS = 27.5;
declare const CLOCK_OUTER_RADIUS = 41.25;
declare const CLOCK_TICK_RADIUS = 7.0833;
type MatClockView = 'hour' | 'minute';
/**
 * A clock that is used as part of the datepicker.
 * @docs-private
 */
declare class MatDatetimepickerClockComponent<D> implements AfterContentInit {
    private _element;
    private _adapter;
    _userSelection: EventEmitter<void>;
    /** A function used to filter which dates are selectable. */
    dateFilter: (date: D, type: MatDatetimepickerFilterType) => boolean;
    interval: number;
    twelvehour: boolean;
    /** Emits when the currently selected date changes. */
    selectedChange: EventEmitter<D>;
    activeDateChange: EventEmitter<D>;
    /** Hours and Minutes representing the clock view. */
    _hours: object[];
    _minutes: object[];
    /** Whether the clock is in hour view. */
    _hourView: boolean;
    _selectedHour: number;
    _selectedMinute: number;
    private _timeChanged;
    private mouseMoveListener;
    private mouseUpListener;
    constructor();
    private _activeDate;
    /**
     * The date to display in this clock view.
     */
    get activeDate(): D;
    set activeDate(value: D);
    private _selected;
    /** The currently selected date. */
    get selected(): D | null;
    set selected(value: D | null);
    private _minDate;
    /** The minimum selectable date. */
    get minDate(): D | null;
    set minDate(value: D | null);
    private _maxDate;
    /** The maximum selectable date. */
    get maxDate(): D | null;
    set maxDate(value: D | null);
    /** Whether the clock should be started in hour or minute view. */
    set startView(value: MatClockView);
    get _hand(): any;
    ngAfterContentInit(): void;
    /** Handles mousedown events on the clock body. */
    _handleMousedown(event: any): void;
    _handleMousemove(event: any): void;
    _handleMouseup(): void;
    /** Initializes this clock view. */
    private _init;
    /**
     * Set Time
     * @param event
     */
    private setTime;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerClockComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<MatDatetimepickerClockComponent<any>, "mat-datetimepicker-clock", never, { "dateFilter": { "alias": "dateFilter"; "required": false; }; "interval": { "alias": "interval"; "required": false; }; "twelvehour": { "alias": "twelvehour"; "required": false; }; "activeDate": { "alias": "activeDate"; "required": false; }; "selected": { "alias": "selected"; "required": false; }; "minDate": { "alias": "minDate"; "required": false; }; "maxDate": { "alias": "maxDate"; "required": false; }; "startView": { "alias": "startView"; "required": false; }; }, { "_userSelection": "_userSelection"; "selectedChange": "selectedChange"; "activeDateChange": "activeDateChange"; }, never, never, false, never>;
}

type MatDatetimepickerType = 'date' | 'time' | 'month' | 'year' | 'datetime';

type MatCalendarView = 'clock' | 'month' | 'year' | 'multi-year';
/**
 * A calendar that is used as part of the datepicker.
 * @docs-private
 */
declare class MatDatetimepickerCalendarComponent<D> implements AfterContentInit, OnDestroy {
    private _elementRef;
    private _intl;
    private _ngZone;
    private _adapter;
    private _dateFormats;
    _userSelection: EventEmitter<void>;
    /** Active multi year view when click on year. */
    multiYearSelector: boolean;
    /** Whether the calendar should be started in month or year view. */
    startView: MatCalendarView;
    twelvehour: boolean;
    timeInterval: number;
    /** A function used to filter which dates are selectable. */
    dateFilter: (date: D, type: MatDatetimepickerFilterType) => boolean;
    ariaLabel: string;
    ariaNextMonthLabel: string;
    ariaPrevMonthLabel: string;
    ariaNextYearLabel: string;
    ariaPrevYearLabel: string;
    ariaNextMultiYearLabel: string;
    ariaPrevMultiYearLabel: string;
    /** Prevent user to select same date time */
    preventSameDateTimeSelection: boolean;
    /** Emits when the currently selected date changes. */
    selectedChange: EventEmitter<D>;
    /** Emits when the view has been changed. **/
    viewChanged: EventEmitter<MatCalendarView>;
    _AMPM: string;
    _clockView: MatClockView;
    _calendarState: string;
    private _intlChanges;
    private _clampedActiveDate;
    constructor();
    private _type;
    get type(): MatDatetimepickerType;
    set type(value: MatDatetimepickerType);
    private _startAt;
    /** A date representing the period (month or year) to start the calendar in. */
    get startAt(): D | null;
    set startAt(value: D | null);
    private _selected;
    /** The currently selected date. */
    get selected(): D | null;
    set selected(value: D | null);
    private _minDate;
    /** The minimum selectable date. */
    get minDate(): D | null;
    set minDate(value: D | null);
    private _maxDate;
    /** The maximum selectable date. */
    get maxDate(): D | null;
    set maxDate(value: D | null);
    /**
     * The current active date. This determines which time period is shown and which date is
     * highlighted when using keyboard navigation.
     */
    get _activeDate(): D;
    set _activeDate(value: D);
    /** Whether the calendar is in month view. */
    _currentView: MatCalendarView;
    get currentView(): MatCalendarView;
    set currentView(view: MatCalendarView);
    /** The label for the current calendar view. */
    get _yearLabel(): string;
    get _monthYearLabel(): string;
    get _dateLabel(): string;
    get _hoursLabel(): string;
    get _minutesLabel(): string;
    get _ariaLabelNext(): string;
    get _ariaLabelPrev(): string;
    /** Date filter for the month and year views. */
    _dateFilterForViews: (date: D) => boolean;
    _userSelected(): void;
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    /** Handles date selection in the month view. */
    _dateSelected(date: D): void;
    /** Handles month selection in the year view. */
    _monthSelected(month: D): void;
    /** Handles year selection in the multi year view. */
    _yearSelected(year: D): void;
    _timeSelected(date: D): void;
    _onActiveDateChange(date: D): void;
    _updateDate(date: D): D;
    _selectAMPM(date: D): void;
    _ampmClicked(source: string): void;
    _yearClicked(): void;
    _dateClicked(): void;
    _hoursClicked(): void;
    _minutesClicked(): void;
    /** Handles user clicks on the previous button. */
    _previousClicked(): void;
    /** Handles user clicks on the next button. */
    _nextClicked(): void;
    /** Whether the previous period button is enabled. */
    _previousEnabled(): boolean;
    /** Whether the next period button is enabled. */
    _nextEnabled(): boolean;
    /** Handles keydown events on the calendar body. */
    _handleCalendarBodyKeydown(event: KeyboardEvent): void;
    _focusActiveCell(): void;
    _calendarStateDone(): void;
    /** Whether the two dates represent the same view in the current view mode (month or year). */
    private _isSameView;
    /** Handles keydown events on the calendar body when calendar is in month view. */
    private _handleCalendarBodyKeydownInMonthView;
    /** Handles keydown events on the calendar body when calendar is in year view. */
    private _handleCalendarBodyKeydownInYearView;
    /** Handles keydown events on the calendar body when calendar is in multi-year view. */
    private _handleCalendarBodyKeydownInMultiYearView;
    /** Handles keydown events on the calendar body when calendar is in month view. */
    private _handleCalendarBodyKeydownInClockView;
    /**
     * Determine the date for the month that comes before the given month in the same column in the
     * calendar table.
     */
    private _prevMonthInSameCol;
    /**
     * Determine the date for the month that comes after the given month in the same column in the
     * calendar table.
     */
    private _nextMonthInSameCol;
    private calendarState;
    private _2digit;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerCalendarComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<MatDatetimepickerCalendarComponent<any>, "mat-datetimepicker-calendar", never, { "multiYearSelector": { "alias": "multiYearSelector"; "required": false; }; "startView": { "alias": "startView"; "required": false; }; "twelvehour": { "alias": "twelvehour"; "required": false; }; "timeInterval": { "alias": "timeInterval"; "required": false; }; "dateFilter": { "alias": "dateFilter"; "required": false; }; "ariaLabel": { "alias": "ariaLabel"; "required": false; }; "ariaNextMonthLabel": { "alias": "ariaNextMonthLabel"; "required": false; }; "ariaPrevMonthLabel": { "alias": "ariaPrevMonthLabel"; "required": false; }; "ariaNextYearLabel": { "alias": "ariaNextYearLabel"; "required": false; }; "ariaPrevYearLabel": { "alias": "ariaPrevYearLabel"; "required": false; }; "ariaNextMultiYearLabel": { "alias": "ariaNextMultiYearLabel"; "required": false; }; "ariaPrevMultiYearLabel": { "alias": "ariaPrevMultiYearLabel"; "required": false; }; "preventSameDateTimeSelection": { "alias": "preventSameDateTimeSelection"; "required": false; }; "type": { "alias": "type"; "required": false; }; "startAt": { "alias": "startAt"; "required": false; }; "selected": { "alias": "selected"; "required": false; }; "minDate": { "alias": "minDate"; "required": false; }; "maxDate": { "alias": "maxDate"; "required": false; }; }, { "_userSelection": "_userSelection"; "selectedChange": "selectedChange"; "viewChanged": "viewChanged"; }, never, never, false, never>;
}

/**
 * An internal class that represents the data corresponding to a single calendar cell.
 * @docs-private
 */
declare class MatDatetimepickerCalendarCell {
    value: number;
    displayValue: string;
    ariaLabel: string;
    enabled: boolean;
    constructor(value: number, displayValue: string, ariaLabel: string, enabled: boolean);
}
/**
 * An internal component used to display calendar data in a table.
 * @docs-private
 */
declare class MatDatetimepickerCalendarBodyComponent {
    /** The label for the table. (e.g. "Jan 2017"). */
    label: string;
    /** The cells to display in the table. */
    rows: MatDatetimepickerCalendarCell[][];
    /** The value in the table that corresponds to today. */
    todayValue: number;
    /** The value in the table that is currently selected. */
    selectedValue: number;
    /** The minimum number of free cells needed to fit the label in the first row. */
    labelMinRequiredCells: number;
    /** The number of columns in the table. */
    numCols: number;
    /** Whether to allow selection of disabled cells. */
    allowDisabledSelection: boolean;
    /** The cell number of the active cell in the table. */
    activeCell: number;
    /** Emits when a new value is selected. */
    selectedValueChange: EventEmitter<number>;
    /** The number of blank cells to put at the beginning for the first row. */
    get _firstRowOffset(): number;
    _cellClicked(cell: MatDatetimepickerCalendarCell): void;
    _isActiveCell(rowIndex: number, colIndex: number): boolean;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerCalendarBodyComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<MatDatetimepickerCalendarBodyComponent, "tbody[matDatetimepickerCalendarBody]", never, { "label": { "alias": "label"; "required": false; }; "rows": { "alias": "rows"; "required": false; }; "todayValue": { "alias": "todayValue"; "required": false; }; "selectedValue": { "alias": "selectedValue"; "required": false; }; "labelMinRequiredCells": { "alias": "labelMinRequiredCells"; "required": false; }; "numCols": { "alias": "numCols"; "required": false; }; "allowDisabledSelection": { "alias": "allowDisabledSelection"; "required": false; }; "activeCell": { "alias": "activeCell"; "required": false; }; }, { "selectedValueChange": "selectedValueChange"; }, never, never, false, never>;
}

declare const MAT_DATETIMEPICKER_VALUE_ACCESSOR: any;
declare const MAT_DATETIMEPICKER_VALIDATORS: any;
/**
 * An event used for datepicker input and change events. We don't always have access to a native
 * input or change event because the event may have been triggered by the user clicking on the
 * calendar popup. For consistency, we always use MatDatepickerInputEvent instead.
 */
declare class MatDatetimepickerInputEvent<D> {
    target: MatDatetimepickerInputDirective<D>;
    targetElement: HTMLElement;
    /** The new value for the target datepicker input. */
    value: D | null;
    constructor(target: MatDatetimepickerInputDirective<D>, targetElement: HTMLElement);
}
/** Directive used to connect an input to a MatDatepicker. */
declare class MatDatetimepickerInputDirective<D> implements AfterContentInit, ControlValueAccessor, OnDestroy, Validator {
    private _elementRef;
    _dateAdapter: DatetimeAdapter<D>;
    private _dateFormats;
    private _formField;
    _datepicker: MatDatetimepickerComponent<D>;
    _dateFilter: (date: D | null, type: MatDatetimepickerFilterType) => boolean;
    /** Emits when a `change` event is fired on this `<input>`. */
    dateChange: EventEmitter<MatDatetimepickerInputEvent<D>>;
    /** Emits when an `input` event is fired on this `<input>`. */
    dateInput: EventEmitter<MatDatetimepickerInputEvent<D>>;
    /** Emits when the value changes (either due to user input or programmatic change). */
    _valueChange: EventEmitter<D>;
    /** Emits when the disabled state has changed */
    _disabledChange: EventEmitter<boolean>;
    private _datepickerSubscription;
    private _localeSubscription;
    /** Whether the last value set on the input was valid. */
    private _lastValueValid;
    constructor();
    /** The datepicker that this input is associated with. */
    set matDatetimepicker(value: MatDatetimepickerComponent<D>);
    set matDatepickerFilter(filter: (date: D | null, type: MatDatetimepickerFilterType) => boolean);
    private _value;
    /** The value of the input. */
    get value(): D | null;
    set value(value: D | null);
    private _min;
    /** The minimum valid date. */
    get min(): D | null;
    set min(value: D | null);
    private _max;
    /** The maximum valid date. */
    get max(): D | null;
    set max(value: D | null);
    private _disabled;
    /** Whether the datepicker-input is disabled. */
    get disabled(): any;
    set disabled(value: any);
    _onTouched: () => void;
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    registerOnValidatorChange(fn: () => void): void;
    validate(c: AbstractControl): ValidationErrors | null;
    /**
     * Gets the element that the datepicker popup should be connected to.
     * @return The element to connect the popup to.
     */
    getConnectedOverlayOrigin(): ElementRef;
    writeValue(value: D): void;
    registerOnChange(fn: (value: any) => void): void;
    registerOnTouched(fn: () => void): void;
    setDisabledState(disabled: boolean): void;
    _onKeydown(event: KeyboardEvent): void;
    _onInput(value: string): void;
    _onChange(): void;
    /** Handles blur events on the input. */
    _onBlur(): void;
    private registerDatepicker;
    private getDisplayFormat;
    private getParseFormat;
    private _cvaOnChange;
    private _validatorOnChange;
    /** The form control validator for whether the input parses. */
    private _parseValidator;
    /** The form control validator for the min date. */
    private _minValidator;
    /** The form control validator for the max date. */
    private _maxValidator;
    /** The form control validator for the date filter. */
    private _filterValidator;
    /** The combined form control validator for this input. */
    private _validator;
    /** Formats a value and sets it on the input element. */
    private _formatValue;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerInputDirective<any>, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<MatDatetimepickerInputDirective<any>, "input[matDatetimepicker]", ["matDatepickerInput"], { "matDatetimepicker": { "alias": "matDatetimepicker"; "required": false; }; "matDatepickerFilter": { "alias": "matDatepickerFilter"; "required": false; }; "value": { "alias": "value"; "required": false; }; "min": { "alias": "min"; "required": false; }; "max": { "alias": "max"; "required": false; }; "disabled": { "alias": "disabled"; "required": false; }; }, { "dateChange": "dateChange"; "dateInput": "dateInput"; }, never, never, false, never>;
}

type MatDatetimepickerMode = 'auto' | 'portrait' | 'landscape';
/**
 * Component used as the content for the datepicker dialog and popup. We use this instead of using
 * MatCalendar directly as the content so we can control the initial focus. This also gives us a
 * place to put additional features of the popup that are not part of the calendar itself in the
 * future. (e.g. confirmation buttons).
 * @docs-private
 */
declare class MatDatetimepickerContentComponent<D> implements AfterContentInit {
    datetimepicker: MatDatetimepickerComponent<D>;
    _calendar: MatDatetimepickerCalendarComponent<D>;
    ngAfterContentInit(): void;
    onSelectionChange(date: D): void;
    /**
     * Handles keydown event on datepicker content.
     * @param event The event.
     */
    _handleKeydown(event: KeyboardEvent): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerContentComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<MatDatetimepickerContentComponent<any>, "mat-datetimepicker-content", never, {}, {}, never, never, false, never>;
}
declare class MatDatetimepickerComponent<D> implements OnDestroy {
    private _dialog;
    private _overlay;
    private _ngZone;
    private _viewContainerRef;
    private _scrollStrategy;
    private _dateAdapter;
    private _dir;
    private _document;
    /** Active multi year view when click on year. */
    multiYearSelector: boolean;
    /** if true change the clock to 12 hour format. */
    twelvehour: boolean;
    /** The view that the calendar should start in. */
    startView: MatCalendarView;
    mode: MatDatetimepickerMode;
    timeInterval: number;
    ariaNextMonthLabel: string;
    ariaPrevMonthLabel: string;
    ariaNextYearLabel: string;
    ariaPrevYearLabel: string;
    /** Prevent user to select same date time */
    preventSameDateTimeSelection: boolean;
    /**
     * Emits new selected date when selected date changes.
     * @deprecated Switch to the `dateChange` and `dateInput` binding on the input element.
     */
    selectedChanged: EventEmitter<D>;
    /** Classes to be passed to the date picker panel. Supports the same syntax as `ngClass`. */
    panelClass: string | string[];
    /** Emits when the datepicker has been opened. */
    openedStream: EventEmitter<void>;
    /** Emits when the datepicker has been closed. */
    closedStream: EventEmitter<void>;
    /** Emits when the view has been changed. **/
    viewChanged: EventEmitter<MatCalendarView>;
    /** Whether the calendar is open. */
    opened: boolean;
    /** The id for the datepicker calendar. */
    id: string;
    /** The input element this datepicker is associated with. */
    _datepickerInput: MatDatetimepickerInputDirective<D>;
    /** Emits when the datepicker is disabled. */
    _disabledChange: Subject<boolean>;
    private _validSelected;
    /** A reference to the overlay when the calendar is opened as a popup. */
    private _popupRef;
    /** A reference to the dialog when the calendar is opened as a dialog. */
    private _dialogRef;
    /** A portal containing the calendar for this datepicker. */
    private _calendarPortal;
    /** The element that was focused before the datepicker was opened. */
    private _focusedElementBeforeOpen;
    private _inputSubscription;
    constructor();
    private _startAt;
    /** The date to open the calendar to initially. */
    get startAt(): D | null;
    set startAt(date: D | null);
    private _openOnFocus;
    get openOnFocus(): boolean;
    set openOnFocus(value: boolean);
    private _type;
    get type(): MatDatetimepickerType;
    set type(value: MatDatetimepickerType);
    private _touchUi;
    /**
     * Whether the calendar UI is in touch mode. In touch mode the calendar opens in a dialog rather
     * than a popup and elements have more padding to allow for bigger touch targets.
     */
    get touchUi(): boolean;
    set touchUi(value: boolean);
    private _disabled;
    /** Whether the datepicker pop-up should be disabled. */
    get disabled(): boolean;
    set disabled(value: boolean);
    /** The currently selected date. */
    get _selected(): D | null;
    set _selected(value: D | null);
    /** The minimum selectable date. */
    get _minDate(): D | null;
    /** The maximum selectable date. */
    get _maxDate(): D | null;
    get _dateFilter(): (date: D | null, type: MatDatetimepickerFilterType) => boolean;
    _handleFocus(): void;
    _viewChanged(type: MatCalendarView): void;
    ngOnDestroy(): void;
    /** Selects the given date */
    _select(date: D): void;
    /**
     * Register an input with this datepicker.
     * @param input The datepicker input to register with this datepicker.
     */
    _registerInput(input: MatDatetimepickerInputDirective<D>): void;
    /** Open the calendar. */
    open(): void;
    /** Close the calendar. */
    close(): void;
    /** Open the calendar as a dialog. */
    private _openAsDialog;
    /** Open the calendar as a popup. */
    private _openAsPopup;
    /** Create the popup. */
    private _createPopup;
    /** Create the popup PositionStrategy. */
    private _createPopupPositionStrategy;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<MatDatetimepickerComponent<any>, "mat-datetimepicker", ["matDatetimepicker"], { "multiYearSelector": { "alias": "multiYearSelector"; "required": false; }; "twelvehour": { "alias": "twelvehour"; "required": false; }; "startView": { "alias": "startView"; "required": false; }; "mode": { "alias": "mode"; "required": false; }; "timeInterval": { "alias": "timeInterval"; "required": false; }; "ariaNextMonthLabel": { "alias": "ariaNextMonthLabel"; "required": false; }; "ariaPrevMonthLabel": { "alias": "ariaPrevMonthLabel"; "required": false; }; "ariaNextYearLabel": { "alias": "ariaNextYearLabel"; "required": false; }; "ariaPrevYearLabel": { "alias": "ariaPrevYearLabel"; "required": false; }; "preventSameDateTimeSelection": { "alias": "preventSameDateTimeSelection"; "required": false; }; "panelClass": { "alias": "panelClass"; "required": false; }; "startAt": { "alias": "startAt"; "required": false; }; "openOnFocus": { "alias": "openOnFocus"; "required": false; }; "type": { "alias": "type"; "required": false; }; "touchUi": { "alias": "touchUi"; "required": false; }; "disabled": { "alias": "disabled"; "required": false; }; }, { "selectedChanged": "selectedChanged"; "openedStream": "opened"; "closedStream": "closed"; "viewChanged": "viewChanged"; }, never, never, false, never>;
}

declare class MatDatetimepickerToggleComponent<D> implements AfterContentInit, OnChanges, OnDestroy {
    _intl: MatDatepickerIntl;
    private _changeDetectorRef;
    /** Datepicker instance that the button will toggle. */
    datetimepicker: MatDatetimepickerComponent<D>;
    private _stateChanges;
    private _disabled;
    /** Whether the toggle button is disabled. */
    get disabled(): boolean;
    set disabled(value: boolean);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    ngAfterContentInit(): void;
    _open(event: Event): void;
    private _watchStateChanges;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerToggleComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<MatDatetimepickerToggleComponent<any>, "mat-datetimepicker-toggle", ["matDatetimepickerToggle"], { "datetimepicker": { "alias": "for"; "required": false; }; "disabled": { "alias": "disabled"; "required": false; }; }, {}, never, never, false, never>;
}

/**
 * An internal component used to display a single month in the datepicker.
 * @docs-private
 */
declare class MatDatetimepickerMonthViewComponent<D> implements AfterContentInit {
    _adapter: DatetimeAdapter<D>;
    private _dateFormats;
    type: MatDatetimepickerType;
    _userSelection: EventEmitter<void>;
    /** A function used to filter which dates are selectable. */
    dateFilter: (date: D) => boolean;
    /** Emits when a new date is selected. */
    selectedChange: EventEmitter<D>;
    /** Grid of calendar cells representing the dates of the month. */
    _weeks: MatDatetimepickerCalendarCell[][];
    /** The number of blank cells in the first row before the 1st of the month. */
    _firstWeekOffset: number;
    /**
     * The date of the month that the currently selected Date falls on.
     * Null if the currently selected Date is in another month.
     */
    _selectedDate: number;
    /** The date of the month that today falls on. Null if today is in another month. */
    _todayDate: number;
    /** The names of the weekdays. */
    _weekdays: {
        long: string;
        narrow: string;
    }[];
    _calendarState: string;
    constructor();
    private _activeDate;
    /**
     * The date to display in this month view (everything other than the month and year is ignored).
     */
    get activeDate(): D;
    set activeDate(value: D);
    private _selected;
    /** The currently selected date. */
    get selected(): D;
    set selected(value: D);
    ngAfterContentInit(): void;
    /** Handles when a new date is selected. */
    _dateSelected(date: number): void;
    _calendarStateDone(): void;
    /** Initializes this month view. */
    private _init;
    /** Creates MdCalendarCells for the dates in this month. */
    private _createWeekCells;
    /**
     * Gets the date in this month that the given Date falls on.
     * Returns null if the given Date is in another month.
     */
    private _getDateInCurrentMonth;
    private calendarState;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerMonthViewComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<MatDatetimepickerMonthViewComponent<any>, "mat-datetimepicker-month-view", never, { "type": { "alias": "type"; "required": false; }; "dateFilter": { "alias": "dateFilter"; "required": false; }; "activeDate": { "alias": "activeDate"; "required": false; }; "selected": { "alias": "selected"; "required": false; }; }, { "_userSelection": "_userSelection"; "selectedChange": "selectedChange"; }, never, never, false, never>;
}

/**
 * An internal component used to display a single year in the datepicker.
 * @docs-private
 */
declare class MatDatetimepickerYearViewComponent<D> implements AfterContentInit {
    _adapter: DatetimeAdapter<D>;
    private _dateFormats;
    _userSelection: EventEmitter<void>;
    type: MatDatetimepickerType;
    /** A function used to filter which dates are selectable. */
    dateFilter: (date: D) => boolean;
    /** Emits when a new month is selected. */
    selectedChange: EventEmitter<D>;
    /** Grid of calendar cells representing the months of the year. */
    _months: MatDatetimepickerCalendarCell[][];
    /** The label for this year (e.g. "2017"). */
    _yearLabel: string;
    /** The month in this year that today falls on. Null if today is in a different year. */
    _todayMonth: number;
    /**
     * The month in this year that the selected Date falls on.
     * Null if the selected Date is in a different year.
     */
    _selectedMonth: number;
    _calendarState: string;
    constructor();
    private _activeDate;
    /** The date to display in this year view (everything other than the year is ignored). */
    get activeDate(): D;
    set activeDate(value: D);
    private _selected;
    /** The currently selected date. */
    get selected(): D;
    set selected(value: D);
    ngAfterContentInit(): void;
    /** Handles when a new month is selected. */
    _monthSelected(month: number): void;
    _calendarStateDone(): void;
    /** Initializes this month view. */
    private _init;
    /**
     * Gets the month in this year that the given Date falls on.
     * Returns null if the given Date is in another year.
     */
    private _getMonthInCurrentYear;
    /** Creates an MdCalendarCell for the given month. */
    private _createCellForMonth;
    /** Whether the given month is enabled. */
    private _isMonthEnabled;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerYearViewComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<MatDatetimepickerYearViewComponent<any>, "mat-datetimepicker-year-view", never, { "type": { "alias": "type"; "required": false; }; "dateFilter": { "alias": "dateFilter"; "required": false; }; "activeDate": { "alias": "activeDate"; "required": false; }; "selected": { "alias": "selected"; "required": false; }; }, { "_userSelection": "_userSelection"; "selectedChange": "selectedChange"; }, never, never, false, never>;
}

declare const yearsPerPage = 24;
declare const yearsPerRow = 4;
/**
 * An internal component used to display multiple years in the datepicker.
 * @docs-private
 */
declare class MatDatetimepickerMultiYearViewComponent<D> implements AfterContentInit {
    _adapter: DatetimeAdapter<D>;
    private _dateFormats;
    _userSelection: EventEmitter<void>;
    type: MatDatetimepickerType;
    /** A function used to filter which dates are selectable. */
    dateFilter: (date: D) => boolean;
    /** Emits when a new month is selected. */
    selectedChange: EventEmitter<D>;
    /** Grid of calendar cells representing the years in the range. */
    _years: MatDatetimepickerCalendarCell[][];
    /** The label for this year range (e.g. "2000-2020"). */
    _yearLabel: string;
    /** The year in this range that today falls on. Null if today is in a different range. */
    _todayYear: number;
    /**
     * The year in this range that the selected Date falls on.
     * Null if the selected Date is in a different range.
     */
    _selectedYear: number | null;
    _calendarState: string;
    constructor();
    private _activeDate;
    /** The date to display in this multi year view*/
    get activeDate(): D;
    set activeDate(value: D);
    private _selected;
    /** The currently selected date. */
    get selected(): D;
    set selected(value: D);
    private _minDate;
    /** The minimum selectable date. */
    get minDate(): D | null;
    set minDate(value: D | null);
    private _maxDate;
    /** The maximum selectable date. */
    get maxDate(): D | null;
    set maxDate(value: D | null);
    ngAfterContentInit(): void;
    /** Handles when a new year is selected. */
    _yearSelected(year: number): void;
    _getActiveCell(): number;
    _calendarStateDone(): void;
    /** Initializes this year view. */
    private _init;
    /** Creates an MatDatetimepickerCalendarCell for the given year. */
    private _createCellForYear;
    /** Whether the given year is enabled. */
    private _shouldEnableYear;
    /**
     * Gets the year in this years range that the given Date falls on.
     * Returns null if the given Date is not in this range.
     */
    private _getYearInCurrentRange;
    /**
     * Validate if the current year is in the current range
     * Returns true if is in range else returns false
     */
    private _isInRange;
    /**
     * @param obj The object to check.
     * @returns The given object if it is both a date instance and valid, otherwise null.
     */
    private _getValidDateOrNull;
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerMultiYearViewComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<MatDatetimepickerMultiYearViewComponent<any>, "mat-datetimepicker-multi-year-view", never, { "type": { "alias": "type"; "required": false; }; "dateFilter": { "alias": "dateFilter"; "required": false; }; "activeDate": { "alias": "activeDate"; "required": false; }; "selected": { "alias": "selected"; "required": false; }; "minDate": { "alias": "minDate"; "required": false; }; "maxDate": { "alias": "maxDate"; "required": false; }; }, { "_userSelection": "_userSelection"; "selectedChange": "selectedChange"; }, never, never, false, never>;
}
declare function isSameMultiYearView<D>(dateAdapter: DatetimeAdapter<D>, date1: D, date2: D, minDate: D | null, maxDate: D | null): boolean;
/**
 * When the multi-year view is first opened, the active year will be in view.
 * So we compute how many years are between the active year and the *slot* where our
 * "startingYear" will render when paged into view.
 */
declare function getActiveOffset<D>(dateAdapter: DatetimeAdapter<D>, activeDate: D, minDate: D | null, maxDate: D | null): number;

declare class MatDatetimepickerModule {
    static ɵfac: i0.ɵɵFactoryDeclaration<MatDatetimepickerModule, never>;
    static ɵmod: i0.ɵɵNgModuleDeclaration<MatDatetimepickerModule, [typeof MatDatetimepickerCalendarComponent, typeof MatDatetimepickerCalendarBodyComponent, typeof MatDatetimepickerClockComponent, typeof MatDatetimepickerComponent, typeof MatDatetimepickerToggleComponent, typeof MatDatetimepickerInputDirective, typeof MatDatetimepickerContentComponent, typeof MatDatetimepickerMonthViewComponent, typeof MatDatetimepickerYearViewComponent, typeof MatDatetimepickerMultiYearViewComponent], [typeof i10.CommonModule, typeof i11.MatButtonModule, typeof i12.MatDialogModule, typeof i13.MatIconModule, typeof i14.OverlayModule, typeof i15.A11yModule], [typeof MatDatetimepickerCalendarComponent, typeof MatDatetimepickerCalendarBodyComponent, typeof MatDatetimepickerClockComponent, typeof MatDatetimepickerComponent, typeof MatDatetimepickerToggleComponent, typeof MatDatetimepickerInputDirective, typeof MatDatetimepickerContentComponent, typeof MatDatetimepickerMonthViewComponent, typeof MatDatetimepickerYearViewComponent, typeof MatDatetimepickerMultiYearViewComponent]>;
    static ɵinj: i0.ɵɵInjectorDeclaration<MatDatetimepickerModule>;
}

export { CLOCK_INNER_RADIUS, CLOCK_OUTER_RADIUS, CLOCK_RADIUS, CLOCK_TICK_RADIUS, DatetimeAdapter, MAT_DATETIMEPICKER_VALIDATORS, MAT_DATETIMEPICKER_VALUE_ACCESSOR, MAT_DATETIME_FORMATS, MAT_NATIVE_DATETIME_FORMATS, MatDatetimepickerCalendarBodyComponent, MatDatetimepickerCalendarCell, MatDatetimepickerCalendarComponent, MatDatetimepickerClockComponent, MatDatetimepickerComponent, MatDatetimepickerContentComponent, MatDatetimepickerFilterType, MatDatetimepickerInputDirective, MatDatetimepickerInputEvent, MatDatetimepickerModule, MatDatetimepickerMonthViewComponent, MatDatetimepickerMultiYearViewComponent, MatDatetimepickerToggleComponent, MatDatetimepickerYearViewComponent, MatNativeDatetimeModule, NativeDatetimeAdapter, NativeDatetimeModule, getActiveOffset, isSameMultiYearView, yearsPerPage, yearsPerRow };
export type { MatCalendarView, MatClockView, MatDatetimeFormats, MatDatetimepickerMode };
