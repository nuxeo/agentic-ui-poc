import * as i0 from '@angular/core';
import { input, signal, computed, numberAttribute, booleanAttribute, output, viewChild, effect, ViewEncapsulation, ChangeDetectionStrategy, Component, inject, HostListener, Directive, ElementRef, afterNextRender, Injectable, ViewContainerRef, ChangeDetectorRef, NgModule } from '@angular/core';
import * as i1 from '@angular/material/button';
import { MatButtonModule } from '@angular/material/button';
import { CdkTrapFocus, isFakeMousedownFromScreenReader } from '@angular/cdk/a11y';
import { TranslatePipe } from '@ngx-translate/core';
import { TemplatePortal } from '@angular/cdk/portal';
import { Subject } from 'rxjs';
import { takeUntil } from 'rxjs/operators';
import { Directionality } from '@angular/cdk/bidi';
import { Overlay, OverlayConfig } from '@angular/cdk/overlay';

/**
 * Throws an exception for the case when tooltip's x-position value isn't valid.
 * In other words, it doesn't match 'before' or 'after'.
 * @docs-private
 */
function throwRichTooltipInvalidPositionX() {
    throw new Error(`positionX value must be either 'before' or 'after'.
    Example: <sat-rich-tooltip positionX="before" #tooltip="satRichTooltip"></sat-rich-tooltip>`);
}
/**
 * Throws an exception for the case when tooltip's y-position value isn't valid.
 * In other words, it doesn't match 'above' or 'below'.
 * @docs-private
 */
function throwRichTooltipInvalidPositionY() {
    throw new Error(`positionY value must be either 'above' or 'below'.
    Example: <sat-rich-tooltip positionY="above" #tooltip="satRichTooltip"></sat-rich-tooltip>`);
}
/**
 * Throws an exception for the case when the trigger doesn't have a valid rich tooltip instance.
 * @docs-private
 */
function throwRichTooltipMissingError() {
    throw new Error(`satRichTooltipTriggerFor: must pass in a rich-tooltip instance.
    Example:
      <sat-rich-tooltip #tooltip="satRichTooltip"></sat-rich-tooltip>
      <button [satRichTooltipTriggerFor]="tooltip"></button>`);
}

/** Counter for generating unique element ids.
 * This is needed for accessibility.
 */
let idCounter = 0;
/**
 * The `SatRichTooltip` is used to display rich tooltips with various configurations.
 * It supports different positions, behaviors, and scroll strategies etc..
 */
class SatRichTooltip {
    /** Closing disabled on richTooltip */
    closeDisabled = false;
    // TODO: DS-915
    /**
     * Unique id for the rich tooltip panel.
     * @deprecated in favor of automatic unique ID generation.
     */
    id = input(...(ngDevMode ? [undefined, { debugName: "id" }] : []));
    _id = `sat-rich-tooltip-${idCounter++}`;
    /**
     * Unique id for the visually hidden transient rich tooltip description element.
     * @internal
     */
    descriptionId = `${this._id}-description`;
    /**
     * Unique id for the title element. Used as the `aria-labelledby` target on the
     * container in persistent mode.
     * @internal
     */
    titleId = `${this._id}-title`;
    /**
     * Unique id for the content element. Used as the `aria-describedby` target on the
     * container in persistent mode.
     * @internal
     */
    contentId = `${this._id}-content`;
    /**
     * Title text registered by `SatRichTooltipTitle` for the hidden description.
     * @internal
     */
    titleDescription = signal('', ...(ngDevMode ? [{ debugName: "titleDescription" }] : []));
    /**
     * Content text registered by `SatRichTooltipContent` for the hidden description.
     * @internal
     */
    contentDescription = signal('', ...(ngDevMode ? [{ debugName: "contentDescription" }] : []));
    /**
     * Plain-text description derived from projected title and content.
     * Used by the visually hidden element for screen reader announcement via aria-describedby.
     * @internal
     */
    descriptionText = computed(() => [this.titleDescription(), this.contentDescription()].join(' '), ...(ngDevMode ? [{ debugName: "descriptionText" }] : []));
    // TODO: DS-915
    /**
     * aria-label for the rich tooltip panel.
     * @deprecated This input is deprecated in favor of a tailored message, and has no effect.
     */
    ariaLabel = input(null, ...(ngDevMode ? [{ debugName: "ariaLabel", alias: 'aria-label' }] : [{ alias: 'aria-label' }]));
    // TODO: DS-915
    /**
     * aria-labelledby for the rich tooltip panel. This needs to be same with the id for the title element.
     * @deprecated This input is deprecated and has no effect.
     */
    ariaLabelledby = input('', ...(ngDevMode ? [{ debugName: "ariaLabelledby", alias: 'aria-labelledby' }] : [{ alias: 'aria-labelledby' }]));
    /**
     * aria-describedby for the rich tooltip panel. This needs to be same with the id for the content element.
     * @deprecated This input is deprecated and has no effect.
     */
    ariaDescribedby = input('', ...(ngDevMode ? [{ debugName: "ariaDescribedby", alias: 'aria-describedby' }] : [{ alias: 'aria-describedby' }]));
    // TODO: DS-915
    /**
     * The ARIA role for the tooltip container.
     * @deprecated The role is now automatically determined by the `behavior` input.
     * Use `behavior="persistent"` for dialog role or `behavior="transient"` for tooltip role.
     * This input will be removed in a future version.
     */
    role = input(undefined, ...(ngDevMode ? [{ debugName: "role" }] : []));
    _roleWarningLogged = false;
    // TODO: DS-915 Rename to "role"
    /**
     * The resolved ARIA role for the tooltip container.
     * Derived from behavior: 'persistent' → 'dialog', 'transient' → 'tooltip'.
     * @internal
     */
    resolvedRole = computed(() => (this.behavior() === 'persistent' ? 'dialog' : 'tooltip'), ...(ngDevMode ? [{ debugName: "resolvedRole" }] : []));
    /**
     * Whether the tooltip has persistent behavior (interactive, click-triggered).
     * @internal
     */
    isPersistent = computed(() => this.behavior() === 'persistent', ...(ngDevMode ? [{ debugName: "isPersistent" }] : []));
    /**
     * Position of the richTooltip in the X axis.
     * @default 'after'
     */
    positionX = input('after', ...(ngDevMode ? [{ debugName: "positionX" }] : []));
    /**
     * Validated position of the richTooltip in the X axis.
     * Throws an error if the position is invalid.
     * @ignore
     */
    validatedPositionX = computed(() => {
        const value = this.positionX();
        if (value !== 'before' && value !== 'after') {
            throwRichTooltipInvalidPositionX();
        }
        return value;
    }, ...(ngDevMode ? [{ debugName: "validatedPositionX" }] : []));
    /**
     * Position of the richTooltip in the Y axis.
     * @default 'below'
     */
    positionY = input('below', ...(ngDevMode ? [{ debugName: "positionY" }] : []));
    /**
     * Validated position of the richTooltip in the Y axis.
     * Throws an error if the position is invalid.
     * @ignore
     */
    validatedPositionY = computed(() => {
        const value = this.positionY();
        if (value !== 'above' && value !== 'below') {
            throwRichTooltipInvalidPositionY();
        }
        return value;
    }, ...(ngDevMode ? [{ debugName: "validatedPositionY" }] : []));
    /**
     * RichTooltip behavior type
     * @default 'transient'
     */
    behavior = input('transient', ...(ngDevMode ? [{ debugName: "behavior" }] : []));
    /**
     * RichTooltip scroll strategy
     * @default 'reposition'
     */
    scrollStrategy = input('reposition', ...(ngDevMode ? [{ debugName: "scrollStrategy" }] : []));
    /**
     * RichTooltip enter delay (milliseconds)
     * @default 200
     */
    showDelay = input(200, ...(ngDevMode ? [{ debugName: "showDelay", transform: numberAttribute }] : [{ transform: numberAttribute }]));
    /**
     * RichTooltip leave delay (milliseconds)
     * @default 1500
     */
    hideDelay = input(1500, ...(ngDevMode ? [{ debugName: "hideDelay", transform: numberAttribute }] : [{ transform: numberAttribute }]));
    /**
     * RichTooltip overlap trigger
     * @default false
     */
    overlapTrigger = input(false, ...(ngDevMode ? [{ debugName: "overlapTrigger", transform: booleanAttribute }] : [{ transform: booleanAttribute }]));
    /**
     * RichTooltip horizontal target offset (px)
     * @default 0
     */
    targetOffsetX = input(0, ...(ngDevMode ? [{ debugName: "targetOffsetX", transform: numberAttribute }] : [{ transform: numberAttribute }]));
    /**
     * RichTooltip vertical target offset (px)
     * @default 20
     */
    targetOffsetY = input(20, ...(ngDevMode ? [{ debugName: "targetOffsetY", transform: numberAttribute }] : [{ transform: numberAttribute }]));
    // TODO: DS-915 Remove
    /**
     * @deprecated The `closeOnClick` input is deprecated and has no effect.
     * Use the `satRichTooltipClose` directive on action buttons to close the tooltip on click instead.
     */
    closeOnClick = input(false, ...(ngDevMode ? [{ debugName: "closeOnClick", transform: booleanAttribute }] : [{ transform: booleanAttribute }]));
    /**
     * Disable animations of richTooltip and all child elements
     * @default false
     */
    disableAnimation = input(false, ...(ngDevMode ? [{ debugName: "disableAnimation", transform: booleanAttribute }] : [{ transform: booleanAttribute }]));
    /**
     * Rich Tooltip focus trap using cdkTrapFocus
     * @default true
     */
    focusTrapEnabled = input(true, ...(ngDevMode ? [{ debugName: "focusTrapEnabled", transform: booleanAttribute }] : [{ transform: booleanAttribute }]));
    /**
     * Rich Tooltip focus trap auto capture using cdkTrapFocusAutoCapture
     * @default true
     */
    focusTrapAutoCaptureEnabled = input(true, ...(ngDevMode ? [{ debugName: "focusTrapAutoCaptureEnabled", transform: booleanAttribute }] : [{ transform: booleanAttribute }]));
    /**
     * Event emitted when the richTooltip is closed.
     */
    closed = output();
    /**
     * Event emitted when the richTooltip is closed.
     *
     * @deprecated Switch to `closed` instead. Will be removed in future version.
     */
    // TODO: DS-915 Remove
    // eslint-disable-next-line @angular-eslint/no-output-native
    close = output();
    /** @ignore */
    templateRef = viewChild('richTooltipContainer', ...(ngDevMode ? [{ debugName: "templateRef" }] : []));
    constructor() {
        // TODO: DS-915 Remove
        // Log deprecation warning if role input is used
        effect(() => {
            if (this.role() !== undefined && !this._roleWarningLogged) {
                this._roleWarningLogged = true;
                console.warn('SatRichTooltip: The "role" input is deprecated and will be removed in a future version. ' +
                    'The role is now automatically determined by the "behavior" input (persistent → dialog, transient → tooltip).');
            }
        });
    }
    /**
     * Handle a keyboard event from the richTooltip, delegating to the appropriate action.
     * @internal
     */
    handleKeydown(event) {
        if (event.key === 'Escape') {
            this.emitCloseEvent('keydown');
        }
    }
    /**
     * This emits a close event to which the trigger is subscribed. When emitted, the
     * trigger will close the richTooltip.
     * @internal
     */
    emitCloseEvent(reason) {
        this.closed.emit(reason);
        // TODO: DS-915 Remove
        this.close.emit(reason);
    }
    /**
     * Prevents closing the Rich Tooltip when the user moves their pointer from the trigger element to the tooltip.
     * @internal
     */
    onMouseover() {
        if (this.behavior() === 'transient') {
            this.closeDisabled = true;
        }
    }
    /**
     * Enables closing of the Rich Tooltip when the user moves their pointer away from the Rich Tooltip element.
     * @internal
     */
    onMouseout() {
        if (this.behavior() === 'transient') {
            this.closeDisabled = false;
            this.emitCloseEvent('focusout');
        }
    }
    /** @ignore */
    doNothing() {
        //NOSONAR
        // Sonar incorrectly flags using mouseover and mouseleave without a focus event listener,
        // but in a transient Rich Tooltip, mouse events must prevent the Rich Tooltip from closing,
        // while focus events are non-existent since transient Rich Tooltips are not focusable.
    }
    ngOnDestroy() {
        this.emitCloseEvent();
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltip, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "20.3.23", type: SatRichTooltip, isStandalone: true, selector: "sat-rich-tooltip", inputs: { id: { classPropertyName: "id", publicName: "id", isSignal: true, isRequired: false, transformFunction: null }, ariaLabel: { classPropertyName: "ariaLabel", publicName: "aria-label", isSignal: true, isRequired: false, transformFunction: null }, ariaLabelledby: { classPropertyName: "ariaLabelledby", publicName: "aria-labelledby", isSignal: true, isRequired: false, transformFunction: null }, ariaDescribedby: { classPropertyName: "ariaDescribedby", publicName: "aria-describedby", isSignal: true, isRequired: false, transformFunction: null }, role: { classPropertyName: "role", publicName: "role", isSignal: true, isRequired: false, transformFunction: null }, positionX: { classPropertyName: "positionX", publicName: "positionX", isSignal: true, isRequired: false, transformFunction: null }, positionY: { classPropertyName: "positionY", publicName: "positionY", isSignal: true, isRequired: false, transformFunction: null }, behavior: { classPropertyName: "behavior", publicName: "behavior", isSignal: true, isRequired: false, transformFunction: null }, scrollStrategy: { classPropertyName: "scrollStrategy", publicName: "scrollStrategy", isSignal: true, isRequired: false, transformFunction: null }, showDelay: { classPropertyName: "showDelay", publicName: "showDelay", isSignal: true, isRequired: false, transformFunction: null }, hideDelay: { classPropertyName: "hideDelay", publicName: "hideDelay", isSignal: true, isRequired: false, transformFunction: null }, overlapTrigger: { classPropertyName: "overlapTrigger", publicName: "overlapTrigger", isSignal: true, isRequired: false, transformFunction: null }, targetOffsetX: { classPropertyName: "targetOffsetX", publicName: "targetOffsetX", isSignal: true, isRequired: false, transformFunction: null }, targetOffsetY: { classPropertyName: "targetOffsetY", publicName: "targetOffsetY", isSignal: true, isRequired: false, transformFunction: null }, closeOnClick: { classPropertyName: "closeOnClick", publicName: "closeOnClick", isSignal: true, isRequired: false, transformFunction: null }, disableAnimation: { classPropertyName: "disableAnimation", publicName: "disableAnimation", isSignal: true, isRequired: false, transformFunction: null }, focusTrapEnabled: { classPropertyName: "focusTrapEnabled", publicName: "focusTrapEnabled", isSignal: true, isRequired: false, transformFunction: null }, focusTrapAutoCaptureEnabled: { classPropertyName: "focusTrapAutoCaptureEnabled", publicName: "focusTrapAutoCaptureEnabled", isSignal: true, isRequired: false, transformFunction: null } }, outputs: { closed: "closed", close: "close" }, viewQueries: [{ propertyName: "templateRef", first: true, predicate: ["richTooltipContainer"], descendants: true, isSignal: true }], exportAs: ["satRichTooltip"], ngImport: i0, template: "<ng-template #richTooltipContainer>\n  <div\n    class=\"sat-rich-tooltip-container mat-elevation-z2\"\n    [attr.tabindex]=\"null\"\n    [attr.role]=\"resolvedRole()\"\n    [attr.aria-modal]=\"isPersistent() ? 'false' : null\"\n    [attr.aria-labelledby]=\"isPersistent() ? titleId : null\"\n    [attr.aria-describedby]=\"isPersistent() ? contentId : null\"\n    [class.sat-rich-tooltip-overlap]=\"overlapTrigger()\"\n    (keydown)=\"handleKeydown($event)\"\n    (mouseover)=\"onMouseover()\"\n    (mouseleave)=\"onMouseout()\"\n    (focus)=\"doNothing()\"\n    [cdkTrapFocus]=\"isPersistent() && focusTrapEnabled()\"\n    [cdkTrapFocusAutoCapture]=\"isPersistent() && focusTrapAutoCaptureEnabled()\"\n    [animate.enter]=\"disableAnimation() ? null : 'sat-rich-tooltip-enter'\"\n    [animate.leave]=\"disableAnimation() ? null : 'sat-rich-tooltip-leave'\"\n  >\n    <ng-content select=\"sat-rich-tooltip-title\"></ng-content>\n    <ng-content select=\"sat-rich-tooltip-content\"></ng-content>\n    <ng-content select=\"sat-rich-tooltip-actions\"></ng-content>\n  </div>\n</ng-template>\n@if (!isPersistent()) {\n  <div class=\"cdk-visually-hidden\" [attr.id]=\"descriptionId\">\n    {{ descriptionText() }}\n  </div>\n}\n", styles: [".sat-rich-tooltip-container{display:grid;padding:12px 0 8px;gap:4px;border-radius:12px;max-width:40vw}@media(max-width:768px){.sat-rich-tooltip-container{max-width:70vw}}@media(max-width:480px){.sat-rich-tooltip-container{max-width:90vw}}.sat-rich-tooltip-title{display:block;align-self:stretch;padding:0 16px}.sat-rich-tooltip-content{display:block;align-self:stretch;padding:0 16px;overflow:auto;max-height:25vh;width:100%;box-sizing:border-box}.sat-rich-tooltip-actions{display:flex;align-items:center;padding:8px 8px 0;gap:8px;align-self:stretch;flex-wrap:wrap}.sat-rich-tooltip-actions.sat-rich-tooltip-actions-align-start{justify-content:start}.sat-rich-tooltip-actions.sat-rich-tooltip-actions-align-center{justify-content:center}.sat-rich-tooltip-actions.sat-rich-tooltip-actions-align-end{justify-content:flex-end}@keyframes sat-rich-tooltip-fade-in{0%{opacity:0}to{opacity:1}}@keyframes sat-rich-tooltip-fade-out{0%{opacity:1}to{opacity:0}}.sat-rich-tooltip-enter{animation:sat-rich-tooltip-fade-in .2s cubic-bezier(.25,.8,.25,1)}.sat-rich-tooltip-leave{animation:sat-rich-tooltip-fade-out 50ms .1s linear both}\n"], dependencies: [{ kind: "directive", type: CdkTrapFocus, selector: "[cdkTrapFocus]", inputs: ["cdkTrapFocus", "cdkTrapFocusAutoCapture"], exportAs: ["cdkTrapFocus"] }], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltip, decorators: [{
            type: Component,
            args: [{ selector: 'sat-rich-tooltip', changeDetection: ChangeDetectionStrategy.OnPush, encapsulation: ViewEncapsulation.None, imports: [CdkTrapFocus], exportAs: 'satRichTooltip', template: "<ng-template #richTooltipContainer>\n  <div\n    class=\"sat-rich-tooltip-container mat-elevation-z2\"\n    [attr.tabindex]=\"null\"\n    [attr.role]=\"resolvedRole()\"\n    [attr.aria-modal]=\"isPersistent() ? 'false' : null\"\n    [attr.aria-labelledby]=\"isPersistent() ? titleId : null\"\n    [attr.aria-describedby]=\"isPersistent() ? contentId : null\"\n    [class.sat-rich-tooltip-overlap]=\"overlapTrigger()\"\n    (keydown)=\"handleKeydown($event)\"\n    (mouseover)=\"onMouseover()\"\n    (mouseleave)=\"onMouseout()\"\n    (focus)=\"doNothing()\"\n    [cdkTrapFocus]=\"isPersistent() && focusTrapEnabled()\"\n    [cdkTrapFocusAutoCapture]=\"isPersistent() && focusTrapAutoCaptureEnabled()\"\n    [animate.enter]=\"disableAnimation() ? null : 'sat-rich-tooltip-enter'\"\n    [animate.leave]=\"disableAnimation() ? null : 'sat-rich-tooltip-leave'\"\n  >\n    <ng-content select=\"sat-rich-tooltip-title\"></ng-content>\n    <ng-content select=\"sat-rich-tooltip-content\"></ng-content>\n    <ng-content select=\"sat-rich-tooltip-actions\"></ng-content>\n  </div>\n</ng-template>\n@if (!isPersistent()) {\n  <div class=\"cdk-visually-hidden\" [attr.id]=\"descriptionId\">\n    {{ descriptionText() }}\n  </div>\n}\n", styles: [".sat-rich-tooltip-container{display:grid;padding:12px 0 8px;gap:4px;border-radius:12px;max-width:40vw}@media(max-width:768px){.sat-rich-tooltip-container{max-width:70vw}}@media(max-width:480px){.sat-rich-tooltip-container{max-width:90vw}}.sat-rich-tooltip-title{display:block;align-self:stretch;padding:0 16px}.sat-rich-tooltip-content{display:block;align-self:stretch;padding:0 16px;overflow:auto;max-height:25vh;width:100%;box-sizing:border-box}.sat-rich-tooltip-actions{display:flex;align-items:center;padding:8px 8px 0;gap:8px;align-self:stretch;flex-wrap:wrap}.sat-rich-tooltip-actions.sat-rich-tooltip-actions-align-start{justify-content:start}.sat-rich-tooltip-actions.sat-rich-tooltip-actions-align-center{justify-content:center}.sat-rich-tooltip-actions.sat-rich-tooltip-actions-align-end{justify-content:flex-end}@keyframes sat-rich-tooltip-fade-in{0%{opacity:0}to{opacity:1}}@keyframes sat-rich-tooltip-fade-out{0%{opacity:1}to{opacity:0}}.sat-rich-tooltip-enter{animation:sat-rich-tooltip-fade-in .2s cubic-bezier(.25,.8,.25,1)}.sat-rich-tooltip-leave{animation:sat-rich-tooltip-fade-out 50ms .1s linear both}\n"] }]
        }], ctorParameters: () => [], propDecorators: { id: [{ type: i0.Input, args: [{ isSignal: true, alias: "id", required: false }] }], ariaLabel: [{ type: i0.Input, args: [{ isSignal: true, alias: "aria-label", required: false }] }], ariaLabelledby: [{ type: i0.Input, args: [{ isSignal: true, alias: "aria-labelledby", required: false }] }], ariaDescribedby: [{ type: i0.Input, args: [{ isSignal: true, alias: "aria-describedby", required: false }] }], role: [{ type: i0.Input, args: [{ isSignal: true, alias: "role", required: false }] }], positionX: [{ type: i0.Input, args: [{ isSignal: true, alias: "positionX", required: false }] }], positionY: [{ type: i0.Input, args: [{ isSignal: true, alias: "positionY", required: false }] }], behavior: [{ type: i0.Input, args: [{ isSignal: true, alias: "behavior", required: false }] }], scrollStrategy: [{ type: i0.Input, args: [{ isSignal: true, alias: "scrollStrategy", required: false }] }], showDelay: [{ type: i0.Input, args: [{ isSignal: true, alias: "showDelay", required: false }] }], hideDelay: [{ type: i0.Input, args: [{ isSignal: true, alias: "hideDelay", required: false }] }], overlapTrigger: [{ type: i0.Input, args: [{ isSignal: true, alias: "overlapTrigger", required: false }] }], targetOffsetX: [{ type: i0.Input, args: [{ isSignal: true, alias: "targetOffsetX", required: false }] }], targetOffsetY: [{ type: i0.Input, args: [{ isSignal: true, alias: "targetOffsetY", required: false }] }], closeOnClick: [{ type: i0.Input, args: [{ isSignal: true, alias: "closeOnClick", required: false }] }], disableAnimation: [{ type: i0.Input, args: [{ isSignal: true, alias: "disableAnimation", required: false }] }], focusTrapEnabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "focusTrapEnabled", required: false }] }], focusTrapAutoCaptureEnabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "focusTrapAutoCaptureEnabled", required: false }] }], closed: [{ type: i0.Output, args: ["closed"] }], close: [{ type: i0.Output, args: ["close"] }], templateRef: [{ type: i0.ViewChild, args: ['richTooltipContainer', { isSignal: true }] }] } });

/**
 * Directive for closing the rich tooltip.
 * This directive listens for click events and triggers the close action on the tooltip.
 */
class SatRichTooltipClose {
    tooltip = inject(SatRichTooltip);
    /**
     * Listens for click events to close the tooltip.
     */
    onClose(event) {
        event.stopPropagation(); // Prevent event bubbling
        this.tooltip.emitCloseEvent(); // Call the close method on the tooltip instance
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipClose, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "20.3.23", type: SatRichTooltipClose, isStandalone: true, selector: "[sat-rich-tooltip-close], [satRichTooltipClose]", host: { listeners: { "click": "onClose($event)" } }, exportAs: ["satRichTooltipClose"], ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipClose, decorators: [{
            type: Directive,
            args: [{
                    selector: '[sat-rich-tooltip-close], [satRichTooltipClose]',
                    exportAs: 'satRichTooltipClose',
                }]
        }], propDecorators: { onClose: [{
                type: HostListener,
                args: ['click', ['$event']]
            }] } });

/**
 * Component for rendering action buttons in the rich tooltip.
 * These action buttons can be aligned to the start, center, or end of the rich tooltip.
 * By default, there will be a close button if the tooltip behaviour is set to be persistent.
 */
class SatRichTooltipActions {
    tooltip = inject(SatRichTooltip);
    _transientWarningLogged = false;
    /**
     * Alignment of the actions
     * @default 'start'
     */
    align = input('start', ...(ngDevMode ? [{ debugName: "align" }] : []));
    // TODO: DS-915 Remove
    /**
     * Whether the close feature should be required
     * @deprecated The close feature is required by default for all persistent tooltips,
     * and must not be used in transient tooltips. This input will be removed in a future
     * release. To ensure accessibility, close buttons are always included in persistent
     * tooltips, and must not be included in transient tooltips.
     */
    requireCloseFeature = input(false, ...(ngDevMode ? [{ debugName: "requireCloseFeature" }] : []));
    constructor() {
        effect(() => {
            if (this.tooltip.behavior() === 'transient' && !this._transientWarningLogged) {
                this._transientWarningLogged = true;
                console.warn('SatRichTooltip: Actions (sat-rich-tooltip-actions) should not be used in transient tooltips. ' +
                    'Transient tooltips are informational only and should not contain interactive elements. ' +
                    'Use behavior="persistent" for tooltips with actions.');
            }
        });
    }
    /** @ignore */
    get tooltipBehaviour() {
        return this.tooltip.behavior();
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipActions, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "20.3.23", type: SatRichTooltipActions, isStandalone: true, selector: "sat-rich-tooltip-actions", inputs: { align: { classPropertyName: "align", publicName: "align", isSignal: true, isRequired: false, transformFunction: null }, requireCloseFeature: { classPropertyName: "requireCloseFeature", publicName: "requireCloseFeature", isSignal: true, isRequired: false, transformFunction: null } }, host: { properties: { "class.sat-rich-tooltip-actions-align-start": "align() === \"start\"", "class.sat-rich-tooltip-actions-align-center": "align() === \"center\"", "class.sat-rich-tooltip-actions-align-end": "align() === \"end\"" }, classAttribute: "sat-rich-tooltip-actions" }, ngImport: i0, template: "<ng-content select=\"[button, a]\"></ng-content>\n\n@if (tooltipBehaviour === 'persistent') {\n  <button mat-button type=\"button\" sat-rich-tooltip-close>Close</button>\n}\n", dependencies: [{ kind: "ngmodule", type: MatButtonModule }, { kind: "component", type: i1.MatButton, selector: "    button[matButton], a[matButton], button[mat-button], button[mat-raised-button],    button[mat-flat-button], button[mat-stroked-button], a[mat-button], a[mat-raised-button],    a[mat-flat-button], a[mat-stroked-button]  ", inputs: ["matButton"], exportAs: ["matButton", "matAnchor"] }, { kind: "directive", type: SatRichTooltipClose, selector: "[sat-rich-tooltip-close], [satRichTooltipClose]", exportAs: ["satRichTooltipClose"] }], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipActions, decorators: [{
            type: Component,
            args: [{ selector: 'sat-rich-tooltip-actions', host: {
                        'class': 'sat-rich-tooltip-actions',
                        '[class.sat-rich-tooltip-actions-align-start]': 'align() === "start"',
                        '[class.sat-rich-tooltip-actions-align-center]': 'align() === "center"',
                        '[class.sat-rich-tooltip-actions-align-end]': 'align() === "end"',
                    }, changeDetection: ChangeDetectionStrategy.OnPush, encapsulation: ViewEncapsulation.None, imports: [MatButtonModule, SatRichTooltipClose], template: "<ng-content select=\"[button, a]\"></ng-content>\n\n@if (tooltipBehaviour === 'persistent') {\n  <button mat-button type=\"button\" sat-rich-tooltip-close>Close</button>\n}\n" }]
        }], ctorParameters: () => [], propDecorators: { align: [{ type: i0.Input, args: [{ isSignal: true, alias: "align", required: false }] }], requireCloseFeature: [{ type: i0.Input, args: [{ isSignal: true, alias: "requireCloseFeature", required: false }] }] } });

/**
 * Component for rendering the content of the rich tooltip.
 */
class SatRichTooltipContent {
    elementRef = inject(ElementRef);
    tooltip = inject(SatRichTooltip);
    /** @internal */
    id = this.tooltip.contentId;
    _observer;
    // TODO: DS-915 Remove
    /** @deprecated This element is not focusable and does not need an aria-label. */
    ariaLabel = input('', ...(ngDevMode ? [{ debugName: "ariaLabel", alias: 'aria-label' }] : [{ alias: 'aria-label' }]));
    constructor() {
        afterNextRender(() => {
            const el = this.elementRef.nativeElement;
            const update = () => this.tooltip.contentDescription.set(el.textContent?.trim() ?? '');
            update();
            this._observer = new MutationObserver(update);
            this._observer.observe(el, { subtree: true, characterData: true, childList: true });
        });
    }
    ngOnDestroy() {
        this._observer?.disconnect();
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipContent, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.1.0", version: "20.3.23", type: SatRichTooltipContent, isStandalone: true, selector: "sat-rich-tooltip-content", inputs: { ariaLabel: { classPropertyName: "ariaLabel", publicName: "aria-label", isSignal: true, isRequired: false, transformFunction: null } }, host: { attributes: { "role": "note" }, properties: { "id": "id" }, classAttribute: "sat-rich-tooltip-content" }, ngImport: i0, template: `
    <ng-content></ng-content>
    <div class="cdk-visually-hidden"> {{'sat.rich-tooltip.press-escape-to-close' | translate}}</div>
  `, isInline: true, dependencies: [{ kind: "pipe", type: TranslatePipe, name: "translate" }], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipContent, decorators: [{
            type: Component,
            args: [{
                    selector: 'sat-rich-tooltip-content',
                    template: `
    <ng-content></ng-content>
    <div class="cdk-visually-hidden"> {{'sat.rich-tooltip.press-escape-to-close' | translate}}</div>
  `,
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    encapsulation: ViewEncapsulation.None,
                    host: {
                        'class': 'sat-rich-tooltip-content',
                        'role': 'note',
                        '[id]': 'id',
                    },
                    imports: [TranslatePipe],
                }]
        }], ctorParameters: () => [], propDecorators: { ariaLabel: [{ type: i0.Input, args: [{ isSignal: true, alias: "aria-label", required: false }] }] } });

/**
 * Component for rendering the title of the rich tooltip.
 */
class SatRichTooltipTitle {
    elementRef = inject(ElementRef);
    tooltip = inject(SatRichTooltip);
    /** @internal */
    id = this.tooltip.titleId;
    _observer;
    // TODO: DS-915 Remove
    /** @deprecated This element is not focusable and does not need an aria-label. */
    ariaLabel = input('', ...(ngDevMode ? [{ debugName: "ariaLabel", alias: 'aria-label' }] : [{ alias: 'aria-label' }]));
    constructor() {
        afterNextRender(() => {
            const el = this.elementRef.nativeElement;
            const update = () => this.tooltip.titleDescription.set(el.textContent?.trim() ?? '');
            update();
            this._observer = new MutationObserver(update);
            this._observer.observe(el, { subtree: true, characterData: true, childList: true });
        });
    }
    ngOnDestroy() {
        this._observer?.disconnect();
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipTitle, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.1.0", version: "20.3.23", type: SatRichTooltipTitle, isStandalone: true, selector: "sat-rich-tooltip-title", inputs: { ariaLabel: { classPropertyName: "ariaLabel", publicName: "aria-label", isSignal: true, isRequired: false, transformFunction: null } }, host: { attributes: { "role": "heading", "aria-level": "2" }, properties: { "id": "id" }, classAttribute: "sat-rich-tooltip-title" }, ngImport: i0, template: `<ng-content></ng-content>`, isInline: true, changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipTitle, decorators: [{
            type: Component,
            args: [{
                    selector: 'sat-rich-tooltip-title',
                    template: `<ng-content></ng-content>`,
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    encapsulation: ViewEncapsulation.None,
                    host: {
                        'class': 'sat-rich-tooltip-title',
                        'role': 'heading',
                        'aria-level': '2',
                        '[id]': 'id',
                    },
                }]
        }], ctorParameters: () => [], propDecorators: { ariaLabel: [{ type: i0.Input, args: [{ isSignal: true, alias: "aria-label", required: false }] }] } });

/** @internal */
class RichTooltipOverlay {
    overlay = inject(Overlay);
    directionality = inject(Directionality);
    /** @ignore */
    // This method creates the overlay from the provided richTooltip's template and saves its OverlayRef so that it can be attached to the DOM when openRichTooltip is called.
    createOverlay(tooltip) {
        return this.overlay.create(this.getOverlayConfig(tooltip));
    }
    /** @ignore */
    // The text direction of the containing app.
    get dir() {
        return this.directionality?.value === 'rtl' ? 'rtl' : 'ltr';
    }
    /** @ignore */
    // This method builds the configuration object needed to create the overlay, the OverlayConfig.
    getOverlayConfig(tooltip) {
        const overlayState = new OverlayConfig();
        /** Display overlay backdrop if trigger event is click */
        if (tooltip.behavior() === 'persistent') {
            overlayState.hasBackdrop = true;
            overlayState.backdropClass = 'cdk-overlay-transparent-backdrop';
        }
        overlayState.direction = this.dir;
        overlayState.scrollStrategy = this.getOverlayScrollStrategy(tooltip.scrollStrategy());
        return overlayState;
    }
    /** @ignore */
    // This method returns the scroll strategy used by the cdk/overlay.
    getOverlayScrollStrategy(strategy) {
        switch (strategy) {
            case 'noop':
                return this.overlay.scrollStrategies.noop();
            case 'close':
                return this.overlay.scrollStrategies.close();
            case 'block':
                return this.overlay.scrollStrategies.block();
            case 'reposition':
            default:
                return this.overlay.scrollStrategies.reposition();
        }
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: RichTooltipOverlay, deps: [], target: i0.ɵɵFactoryTarget.Injectable });
    static ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: RichTooltipOverlay });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: RichTooltipOverlay, decorators: [{
            type: Injectable
        }] });

/** @internal */
class RichTooltipPosition {
    overlay = inject(Overlay);
    /**
     * This method builds the position strategy for the overlay, so the richTooltip is properly connected
     * to the trigger.
     * @ignore
     * @returns ConnectedPositionStrategy
     */
    getPosition(tooltip, elementRef) {
        const [originX, originFallbackX] = tooltip.validatedPositionX() === 'before' ? ['end', 'start'] : ['start', 'end'];
        const [overlayY, overlayFallbackY] = tooltip.validatedPositionY() === 'above' ? ['bottom', 'top'] : ['top', 'bottom'];
        let originY = overlayY;
        let originFallbackY = overlayFallbackY;
        const overlayX = originX;
        const overlayFallbackX = originFallbackX;
        let offsetX = 0;
        let offsetY = 0;
        /**
         * Reverse overlayY and fallbackOverlayY when overlapTrigger is false.
         * Also update the horizontal and vertical offset
         */
        if (!tooltip.overlapTrigger()) {
            originY = overlayY === 'top' ? 'bottom' : 'top';
            originFallbackY = overlayFallbackY === 'top' ? 'bottom' : 'top';
            if (tooltip.targetOffsetX() && !Number.isNaN(Number(tooltip.targetOffsetX()))) {
                const targetOffsetX = Number(tooltip.targetOffsetX());
                // Need to have a horizontal offset if provided by the input
                if (originX === 'start')
                    offsetX = targetOffsetX;
                // Need to have the horizontal offset in the negative direction
                else
                    offsetX = -targetOffsetX;
            }
            if (tooltip.targetOffsetY() && !Number.isNaN(Number(tooltip.targetOffsetY()))) {
                const targetOffsetY = Number(tooltip.targetOffsetY());
                // Need to have a vertical offset if provided by the input
                if (originY === 'bottom')
                    offsetY = targetOffsetY;
                // Need to have the vertical offset in the negative direction
                else
                    offsetY = -targetOffsetY;
            }
        }
        return this.overlay
            .position()
            .flexibleConnectedTo(elementRef)
            .withLockedPosition(true)
            .withPush(true) // Enables pushing the overlay into the viewport
            .withViewportMargin(8) // Set an 8px margin to keep the overlay from touching the viewport edges.
            .withPositions([
            {
                originX,
                originY,
                overlayX,
                overlayY,
                offsetY,
            },
            {
                originX: originFallbackX,
                originY,
                overlayX: overlayFallbackX,
                overlayY,
                offsetY,
            },
            {
                originX,
                originY: originFallbackY,
                overlayX,
                overlayY: overlayFallbackY,
                offsetY: -offsetY,
            },
            {
                originX: originFallbackX,
                originY: originFallbackY,
                overlayX: overlayFallbackX,
                overlayY: overlayFallbackY,
                offsetY: -offsetY,
            },
        ])
            .withDefaultOffsetX(offsetX)
            .withDefaultOffsetY(offsetY);
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: RichTooltipPosition, deps: [], target: i0.ɵɵFactoryTarget.Injectable });
    static ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: RichTooltipPosition });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: RichTooltipPosition, decorators: [{
            type: Injectable
        }] });

/**
 * Directive for triggering the rich tooltip.
 * This directive listens for various events (click, mouseenter, focus, mouseleave) to control the opening and closing of the tooltip.
 */
class SatRichTooltipTrigger {
    elementRef = inject(ElementRef);
    viewContainerRef = inject(ViewContainerRef);
    richTooltipOverlayService = inject(RichTooltipOverlay);
    richTooltipPositionService = inject(RichTooltipPosition);
    changeDetectorRef = inject(ChangeDetectorRef);
    /** Whether the rich tooltip is currently open. */
    richTooltipOpen = signal(false, ...(ngDevMode ? [{ debugName: "richTooltipOpen" }] : []));
    richTooltipOpened = new Subject();
    richTooltipClosed = new Subject();
    portal;
    overlayRef = null;
    halt = false; // Restricts the tooltip to be triggered multiple time if there is a delay while opening it.
    backdropSubscription;
    positionSubscription;
    detachmentsSubscription;
    mouseoverTimer;
    openedByMouse = false;
    /** References the richTooltip instance that the trigger is associated with. */
    tooltip = input.required(...(ngDevMode ? [{ debugName: "tooltip", alias: 'satRichTooltipTriggerFor' }] : [{ alias: 'satRichTooltipTriggerFor' }]));
    /**
     * Computes the aria-haspopup value based on the tooltip's behavior.
     * Returns "dialog" when behavior is "persistent", otherwise undefined (no attribute) for transient tooltips.
     * @ignore
     */
    ariaHaspopup = computed(() => {
        return this.tooltip().isPersistent() ? 'dialog' : null;
    }, ...(ngDevMode ? [{ debugName: "ariaHaspopup" }] : []));
    /**
     * Computes the aria-expanded value based on the tooltip's behavior.
     * Returns the open state when behavior is "persistent", otherwise undefined (no attribute) for transient tooltips.
     * Per ARIA best practices, aria-expanded is appropriate for dialog triggers but not for tooltip triggers.
     * @ignore
     */
    ariaExpanded = computed(() => {
        return this.tooltip().isPersistent() ? this.richTooltipOpen() : null;
    }, ...(ngDevMode ? [{ debugName: "ariaExpanded" }] : []));
    ariaDescribedby = computed(() => this.tooltip().isPersistent() ? null : this.tooltip().descriptionId, ...(ngDevMode ? [{ debugName: "ariaDescribedby" }] : []));
    /** RichTooltip won't open on trigger */
    satRichTooltipDisabled = input(false, ...(ngDevMode ? [{ debugName: "satRichTooltipDisabled", transform: booleanAttribute }] : [{ transform: booleanAttribute }]));
    /** RichTooltip backdrop close on click */
    satRichTooltipBackdropCloseOnClick = input(true, ...(ngDevMode ? [{ debugName: "satRichTooltipBackdropCloseOnClick", transform: booleanAttribute }] : [{
            transform: booleanAttribute,
        }]));
    /** Event emitted when the associated richTooltip is opened. */
    tooltipOpened = output();
    /** Event emitted when the associated richTooltip is closed. */
    tooltipClosed = output();
    /** @ignore */
    ngAfterViewInit() {
        this.checkRichTooltip();
        this.tooltip().closed.subscribe(() => this.closeRichTooltip());
    }
    /**
     * Listens for click events to toggle the rich tooltip only if the behavior is persistent.
     * @param event The click event.
     */
    onClick(event) {
        if (event && this.tooltip().behavior() === 'persistent') {
            this.toggleRichTooltip();
        }
    }
    /**
     * Listens for mouse enter events to open the rich tooltip only if the behavior is transient.
     * @param event The mouse enter event.
     */
    onMouseEnter(event) {
        if (event && !this.richTooltipOpen() && this.tooltip().behavior() === 'transient') {
            this.checkTooltipToOpen(true);
        }
    }
    /**
     * Listens for focus events to open the rich tooltip only if the behavior is transient.
     * @param event The focus event.
     */
    onFocus(event) {
        if (event && !this.richTooltipOpen() && this.tooltip().behavior() === 'transient') {
            this.checkTooltipToOpen();
        }
    }
    /**
     * Listens for focusout events to close the rich tooltip only if the behavior is transient.
     * @param event The focus event.
     */
    onFocusOut(event) {
        if (event && this.richTooltipOpen() && this.tooltip().behavior() === 'transient') {
            setTimeout(() => {
                if (!this.tooltip().closeDisabled) {
                    this.closeRichTooltip();
                }
            }, this.tooltip().hideDelay());
        }
    }
    /** Listens for Escape key down on the trigger to close the Rich Tooltip. */
    onEscapeKey() {
        if (this.richTooltipOpen()) {
            this.closeRichTooltip();
        }
    }
    /**
     * Listens for mouse leave events to close the rich tooltip only if the behavior is transient.
     * @param event The mouse leave event.
     */
    onMouseLeave(event) {
        if (event && this.tooltip().behavior() === 'transient') {
            if (this.richTooltipOpen()) {
                if (this.mouseoverTimer) {
                    clearTimeout(this.mouseoverTimer);
                    this.mouseoverTimer = null;
                }
                setTimeout(() => {
                    if (!this.tooltip().closeDisabled) {
                        this.closeRichTooltip();
                    }
                }, this.tooltip().hideDelay());
            }
        }
    }
    /** @ignore */
    checkTooltipToOpen(openedByMouse = false) {
        if (!this.halt) {
            this.halt = true;
            this.openedByMouse = openedByMouse;
            this.mouseoverTimer = setTimeout(() => {
                this.openRichTooltip();
            }, this.tooltip().showDelay());
        }
    }
    /**
     * Listens for mousedown events to track if the tooltip was opened by mouse interaction.
     * @param event The mousedown event.
     */
    handleMousedown(event) {
        if (event && !isFakeMousedownFromScreenReader(event)) {
            this.openedByMouse = true;
        }
    }
    /** @ignore */
    // Toggles the richTooltip between the open and closed states.
    toggleRichTooltip() {
        return this.richTooltipOpen() ? this.closeRichTooltip() : this.openRichTooltip();
    }
    /** @ignore */
    // Opens the richTooltip.
    openRichTooltip() {
        if (!this.satRichTooltipDisabled() && !this.richTooltipOpen()) {
            const positionStrategy = this.richTooltipPositionService.getPosition(this.tooltip(), this.elementRef);
            if (!this.overlayRef) {
                const templateRef = this.tooltip().templateRef();
                if (templateRef)
                    this.portal = new TemplatePortal(templateRef, this.viewContainerRef);
                this.overlayRef = this.richTooltipOverlayService.createOverlay(this.tooltip());
                this.overlayRef.updatePositionStrategy(positionStrategy);
            }
            this.overlayRef.attach(this.portal);
            // required for ChangeDetectionStrategy.OnPush
            this.changeDetectorRef.markForCheck();
            this.subscribeToBackdrop();
            this.subscribeToDetachments();
            this.initRichTooltip();
        }
    }
    /** @ignore */
    // Closes the richTooltip.
    closeRichTooltip() {
        this.overlayRef?.detach();
        this.changeDetectorRef.markForCheck();
        this.resetRichTooltip();
    }
    /** @ignore */
    // Removes the richTooltip from the DOM.
    destroyRichTooltip() {
        if (this.mouseoverTimer) {
            clearTimeout(this.mouseoverTimer);
            this.mouseoverTimer = null;
        }
        if (this.overlayRef) {
            this.overlayRef.dispose();
            this.overlayRef = null;
            this.cleanUpSubscriptions();
        }
        this.richTooltipOpened.complete();
        this.richTooltipClosed.complete();
    }
    /** @ignore */
    // Focuses the richTooltip trigger.
    focus() {
        this.elementRef.nativeElement.focus();
    }
    /** @ignore */
    // This method ensures that the richTooltip closes when the overlay backdrop is clicked. We do not use first() here because doing so would not catch clicks from within the richTooltip, and it would fail to unsubscribe properly. Instead, we unsubscribe explicitly when the richTooltip is closed or destroyed.
    subscribeToBackdrop() {
        // Only subscribe to backdrop if trigger event is click
        if (this.overlayRef) {
            if (this.tooltip().behavior() === 'persistent' &&
                this.satRichTooltipBackdropCloseOnClick() === true) {
                this.overlayRef
                    .backdropClick()
                    .pipe(takeUntil(this.richTooltipClosed))
                    .subscribe(() => {
                    this.tooltip().emitCloseEvent();
                });
            }
        }
    }
    /** @ignore */
    subscribeToDetachments() {
        if (this.overlayRef) {
            this.overlayRef
                .detachments()
                .pipe(takeUntil(this.richTooltipClosed))
                .subscribe(() => {
                this.setRichTooltipClosed();
            });
        }
    }
    /** @ignore */
    // This method sets the richTooltip state to open and focuses the first item if the richTooltip was opened via the keyboard.
    initRichTooltip() {
        this.setRichTooltipOpened();
    }
    /** @ignore */
    // This method resets the richTooltip when it's closed, most importantly restoring focus to the richTooltip trigger if the richTooltip was opened via the keyboard.
    resetRichTooltip() {
        this.setRichTooltipClosed();
        // Focus only needs to be reset to the host element if the richTooltip was opened by the keyboard and behavior is persistent.
        if (!this.openedByMouse && this.tooltip().behavior() === 'persistent') {
            this.focus();
        }
        this.openedByMouse = false;
    }
    /** @ignore */
    // set state rather than toggle to support triggers sharing a richTooltip
    setRichTooltipOpened() {
        if (!this.richTooltipOpen()) {
            this.richTooltipOpen.set(true);
            this.halt = false;
            this.richTooltipOpened.next();
            this.tooltipOpened.emit();
        }
    }
    /** @ignore */
    // set state rather than toggle to support triggers sharing a richTooltip
    setRichTooltipClosed() {
        if (this.richTooltipOpen()) {
            this.richTooltipOpen.set(false);
            this.richTooltipClosed.next();
            this.tooltipClosed.emit();
        }
    }
    /** @ignore */
    // This method checks that a valid instance of MdRichTooltip has been passed into satRichTooltipTriggerFor. If not, an exception is thrown.
    checkRichTooltip() {
        if (!this.tooltip()) {
            throwRichTooltipMissingError();
        }
    }
    /** @ignore */
    cleanUpSubscriptions() {
        if (this.backdropSubscription) {
            this.backdropSubscription.unsubscribe();
        }
        if (this.positionSubscription) {
            this.positionSubscription.unsubscribe();
        }
        if (this.detachmentsSubscription) {
            this.detachmentsSubscription.unsubscribe();
        }
    }
    /** @ignore */
    ngOnDestroy() {
        this.destroyRichTooltip();
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipTrigger, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "20.3.23", type: SatRichTooltipTrigger, isStandalone: true, selector: "[satRichTooltipTriggerFor]", inputs: { tooltip: { classPropertyName: "tooltip", publicName: "satRichTooltipTriggerFor", isSignal: true, isRequired: true, transformFunction: null }, satRichTooltipDisabled: { classPropertyName: "satRichTooltipDisabled", publicName: "satRichTooltipDisabled", isSignal: true, isRequired: false, transformFunction: null }, satRichTooltipBackdropCloseOnClick: { classPropertyName: "satRichTooltipBackdropCloseOnClick", publicName: "satRichTooltipBackdropCloseOnClick", isSignal: true, isRequired: false, transformFunction: null } }, outputs: { tooltipOpened: "tooltipOpened", tooltipClosed: "tooltipClosed" }, host: { listeners: { "click": "onClick($event)", "mouseenter": "onMouseEnter($event)", "focus": "onFocus($event)", "focusout": "onFocusOut($event)", "keydown.escape": "onEscapeKey()", "mouseleave": "onMouseLeave($event)", "mousedown": "handleMousedown($event)" }, properties: { "class.sat-rich-tooltip-disabled": "satRichTooltipDisabled()", "attr.aria-haspopup": "ariaHaspopup()", "attr.aria-expanded": "ariaExpanded()", "attr.aria-describedby": "ariaDescribedby()" }, classAttribute: "sat-rich-tooltip-trigger" }, providers: [RichTooltipOverlay, RichTooltipPosition], exportAs: ["satRichTooltipTrigger"], ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipTrigger, decorators: [{
            type: Directive,
            args: [{
                    selector: `[satRichTooltipTriggerFor]`,
                    providers: [RichTooltipOverlay, RichTooltipPosition],
                    host: {
                        'class': 'sat-rich-tooltip-trigger',
                        '[class.sat-rich-tooltip-disabled]': 'satRichTooltipDisabled()',
                        '[attr.aria-haspopup]': 'ariaHaspopup()',
                        '[attr.aria-expanded]': 'ariaExpanded()',
                        '[attr.aria-describedby]': 'ariaDescribedby()',
                    },
                    exportAs: 'satRichTooltipTrigger',
                }]
        }], propDecorators: { tooltip: [{ type: i0.Input, args: [{ isSignal: true, alias: "satRichTooltipTriggerFor", required: true }] }], satRichTooltipDisabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "satRichTooltipDisabled", required: false }] }], satRichTooltipBackdropCloseOnClick: [{ type: i0.Input, args: [{ isSignal: true, alias: "satRichTooltipBackdropCloseOnClick", required: false }] }], tooltipOpened: [{ type: i0.Output, args: ["tooltipOpened"] }], tooltipClosed: [{ type: i0.Output, args: ["tooltipClosed"] }], onClick: [{
                type: HostListener,
                args: ['click', ['$event']]
            }], onMouseEnter: [{
                type: HostListener,
                args: ['mouseenter', ['$event']]
            }], onFocus: [{
                type: HostListener,
                args: ['focus', ['$event']]
            }], onFocusOut: [{
                type: HostListener,
                args: ['focusout', ['$event']]
            }], onEscapeKey: [{
                type: HostListener,
                args: ['keydown.escape']
            }], onMouseLeave: [{
                type: HostListener,
                args: ['mouseleave', ['$event']]
            }], handleMousedown: [{
                type: HostListener,
                args: ['mousedown', ['$event']]
            }] } });

const satRichTooltipArtifacts = [
    SatRichTooltip,
    SatRichTooltipTrigger,
    SatRichTooltipTitle,
    SatRichTooltipContent,
    SatRichTooltipActions,
    SatRichTooltipClose,
];
class SatRichTooltipModule {
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule });
    static ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipModule, imports: [SatRichTooltip,
            SatRichTooltipTrigger,
            SatRichTooltipTitle,
            SatRichTooltipContent,
            SatRichTooltipActions,
            SatRichTooltipClose], exports: [SatRichTooltip,
            SatRichTooltipTrigger,
            SatRichTooltipTitle,
            SatRichTooltipContent,
            SatRichTooltipActions,
            SatRichTooltipClose] });
    static ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipModule, imports: [SatRichTooltipActions] });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatRichTooltipModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: satRichTooltipArtifacts,
                    exports: satRichTooltipArtifacts,
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { SatRichTooltip, SatRichTooltipActions, SatRichTooltipActions as SatRichTooltipActionsComponent, SatRichTooltipClose, SatRichTooltipClose as SatRichTooltipCloseDirective, SatRichTooltip as SatRichTooltipComponent, SatRichTooltipContent, SatRichTooltipContent as SatRichTooltipContentComponent, SatRichTooltipModule, SatRichTooltipTitle, SatRichTooltipTitle as SatRichTooltipTitleComponent, SatRichTooltipTrigger, SatRichTooltipTrigger as SatRichTooltipTriggerDirective };
//# sourceMappingURL=hylandsoftware-satori-ui-rich-tooltip.mjs.map
