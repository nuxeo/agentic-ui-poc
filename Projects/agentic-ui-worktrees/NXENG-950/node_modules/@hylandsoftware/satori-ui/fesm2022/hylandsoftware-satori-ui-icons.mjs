import { SATORI_ICONS } from '@hylandsoftware/satori-icons';
export { SATORI_ICONS } from '@hylandsoftware/satori-icons';
import * as i0 from '@angular/core';
import { inject, NgModule, makeEnvironmentProviders, provideAppInitializer } from '@angular/core';
import { MatIconRegistry } from '@angular/material/icon';
import { DomSanitizer } from '@angular/platform-browser';
import { DOCUMENT } from '@angular/common';

/**
 * @deprecated Use provideSatoriIcons from @hylandsoftware/satori-ui/icons instead
 */
class SatIconModule {
    constructor() {
        const registry = inject(MatIconRegistry);
        const sanitizer = inject(DomSanitizer);
        registry.addSvgIconSetLiteral(sanitizer.bypassSecurityTrustHtml(SATORI_ICONS)); // NOSONAR Safe operation using trusted Satori artifact
        // support temporary _24px suffixed icons name
        // TODO: major release: remove support for _24px suffixed icons name
        registry.addSvgIconSetLiteral(sanitizer.bypassSecurityTrustHtml(SATORI_ICONS.replaceAll(/id="([^"]*)"/gi, 'id="$1_24px"')));
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatIconModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule });
    static ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "20.3.23", ngImport: i0, type: SatIconModule });
    static ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatIconModule });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.23", ngImport: i0, type: SatIconModule, decorators: [{
            type: NgModule
        }], ctorParameters: () => [] });

function provideSatoriIcons() {
    return makeEnvironmentProviders([
        provideAppInitializer(() => {
            const registry = inject(MatIconRegistry);
            const sanitizer = inject(DomSanitizer);
            const document = inject(DOCUMENT);
            registry.addSvgIconSetLiteral(sanitizer.bypassSecurityTrustHtml(SATORI_ICONS));
            const errorSvg = /<svg id="error"[^>]*>[\s\S]*?<\/svg>/.exec(SATORI_ICONS)?.[0] ?? null;
            if (errorSvg) {
                const standaloneSvg = errorSvg.includes('xmlns=')
                    ? errorSvg
                    : errorSvg.replace('<svg', '<svg xmlns="http://www.w3.org/2000/svg"');
                document.documentElement.classList.add('sat-form-field-error-icon');
                document.documentElement.style.setProperty('--sat-form-field-error-mask-image', `url("data:image/svg+xml,${encodeURIComponent(standaloneSvg)}")`);
            }
        }),
    ]);
}

/**
 * Generated bundle index. Do not edit.
 */

export { SatIconModule, provideSatoriIcons };
//# sourceMappingURL=hylandsoftware-satori-ui-icons.mjs.map
