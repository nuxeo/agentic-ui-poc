<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.nuxeo.agentic</groupId>
    <artifactId>nuxeo-agentic-ui-parent</artifactId>
    <version>2026.0.1-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>nuxeo-agentic-ui</artifactId>
  <packaging>pom</packaging>
  <name>Nuxeo Agentic UI - Frontend</name>

  <build>
    <plugins>
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>1.15.0</version>
        <configuration>
          <nodeVersion>${node.version}</nodeVersion>
          <npmVersion>${npm.version}</npmVersion>
          <!-- Nx monorepo: npm commands run from workspace root -->
          <workingDirectory>${project.basedir}/../..</workingDirectory>
        </configuration>
        <executions>
          <execution>
            <id>install node and npm</id>
            <goals><goal>install-node-and-npm</goal></goals>
          </execution>
          <execution>
            <id>npm install</id>
            <phase>process-resources</phase>
            <configuration><arguments>ci</arguments></configuration>
            <goals><goal>npm</goal></goals>
          </execution>
          <execution>
            <id>npm build</id>
            <phase>compile</phase>
            <configuration>
              <arguments>run build -- --project=nuxeo-ui --configuration=production --base-href=/nuxeo/agentic-ui/</arguments>
            </configuration>
            <goals><goal>npm</goal></goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
