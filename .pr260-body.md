## What changed and why

IBM issue **3215704996** flagged the 11px `.format-type` label at `#999` (~2.85:1). The **production SCSS fix** (themed `.picture-cards` + `--mat-sys-on-surface-variant` on `.format-type`) is on `main` from NXENG-856. This PR adds **NXENG-901 regression coverage**: a Karma assertion that the label consumes `--mat-sys-on-surface-variant`, plus the existing per-theme ≥4.5:1 contrast matrix on the real `DocumentViewerComponent`.

## JIRA ticket

[NXENG-901](https://hyland.atlassian.net/browse/NXENG-901)

## Acceptance criteria

1. **Contrast remediated** — Karma per-theme contrast on `.picture-cards`; Vitest SCSS pairing on `main` (`document-viewer-format-type-contrast.spec.ts` in `libs/shared/ui`).
2. **IBM 3215704996** — addressed at source on `main`; this PR locks token wiring under the NXENG-901 ticket id.
3. **No layout regression** — same viewer fixture as NXENG-856 contrast suite.

## Files modified

- `apps/nuxeo-ui/src/app/document-viewer/document-viewer-format-type-contrast.spec.ts`

## How to test

- [ ] `npx nx test nuxeo-ui --include=**/document-viewer-format-type-contrast.spec.ts`
- [ ] `npx nx test ui --testFile=document-viewer-format-type-contrast.spec.ts`
- [ ] Picture doc **View** tab → `.format-type` readable; devtools shows theme token, not `rgb(153, 153, 153)`

## Blast radius

Test-only delta vs `main` (no SCSS change on this branch).

## Checklist

- [x] `npx nx test nuxeo-ui` / `ui` contrast specs pass
- [x] `npm run review:guardrails` on push
