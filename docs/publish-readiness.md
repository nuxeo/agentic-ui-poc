# Publishing `@nuxeo-satori/platform` — what is left, and who decides

> **SUPERSEDED, for the scope decision only.** The scope is settled:
> **`@nuxeo/satori-platform` on `https://packages.nuxeo.com/repository/npm-public/`**,
> the registry `nuxeo-elements` already publishes to. The runbook and the secrets live
> in **[`publishing-to-nuxeo-registry.md`](publishing-to-nuxeo-registry.md)**.
>
> Two claims in §1 below were wrong and are kept only so the reasoning is auditable:
> it recommended GitHub Packages, and it called `@nuxeo` an actively published _public
> npm_ scope. `@nuxeo` publishes to Nuxeo Nexus; public npm holds only a legacy
> `3.0.13` from the 3.x era against a live `2025.18.0`. Checking the version number
> would have caught it.
>
> §§2–5 — what a scope change costs, what is already proven, and what is deliberately
> not done — remain accurate, with one correction recorded in §3.

**Status:** scope decided; publishing deliberately deferred to the final deployment
stage. `"private": true` is still in place and nothing publishes.

The package builds, installs from a tarball, resolves every subpath, compiles a real
application against its published types, and — since 2026-08-24 — **survives an actual
`npm publish --dry-run`**. Until that date it did not: see §3.

---

## 1. The decision — superseded, see the note above

`@nuxeo-satori` is **unclaimed** on public npm — `npm view @nuxeo-satori/platform`
returns 404 and no package exists under the scope. So nothing is blocked by a name
collision; the question is which registry and scope the Beta should ship under.

### Option A — GitHub Packages under an owned scope (recommended at the time)

```jsonc
// libs/platform/package.json
"publishConfig": { "registry": "https://npm.pkg.github.com" }
```

**Why this first:** the repo already does exactly this in both directions.
`.npmrc` maps `@alfresco` and `@hylandsoftware` to `npm.pkg.github.com`, and CI already
holds `SATORI_GH_READONLY_TOKEN`. A Beta with named customers wants an access-controlled
registry anyway, and GitHub Packages requires the scope to match the owning
org — so an existing org is a scope we demonstrably own.

**Cost:** customers need a token to install, and a `.npmrc` line. For a private Beta
that is a feature, not friction.

### Option B — public npm under `@nuxeo`

`@nuxeo` is an **existing, owned public scope** — `@nuxeo/nuxeo-ui-elements` and
`@nuxeo/nuxeo` ship from it today. So `@nuxeo/satori-platform` needs publish rights on
an org that already exists, and no new org registration.

**Cost:** public. Anything published is world-readable and cannot be truly unpublished
after 72 hours.

### Option C — register `@nuxeo-satori` on public npm

What the plan originally proposed. It is available, but it means creating and owning a
new org, and it is the only option that adds an entity to maintain.

### Recommendation

**Option A for Beta, Option B at GA.** It matches the access model a Beta wants, uses
infrastructure that already works in this repo, and defers the public-name decision
until the API surface has stopped moving. Switching later is one `name` field and a
`publishConfig` block.

---

## 2. Changing the scope is a small, mechanical change

The package name is authored in two files, and only one of them is a single line:

| File                         | What it holds                                      | Edit size  |
| ---------------------------- | -------------------------------------------------- | ---------- |
| `libs/platform/package.json` | `"name": "@nuxeo-satori/platform"`                 | one line   |
| `tsconfig.base.json`         | five alias keys mapping the entry points to source | five lines |

Everything downstream is derived:

- the generated `exports` map and `.d.ts` file names — ng-packagr, from `name`
- `docs/api/platform.api.md` — regenerated by `npm run beta:api -- --update`
- `scripts/beta-harness/fork-simulation.mjs` — reads `name` from the built
  `package.json` and repoints aliases by prefix, so it needs no edit

The **import specifiers** in source (`@nuxeo-satori/platform/extensions`) and their
`tsconfig.base.json` aliases do need a rename, which is a mechanical find-and-replace
across 190 sites — the same operation already done once, verified by running
`typecheck build test` across all projects.

> `fork-simulation` fails loudly if no alias matches the package name, rather than
> compiling against the source tree and reporting a vacuous pass. So a half-finished
> scope rename cannot look green.

---

## 3. What is already proven

| Claim                                            | How                                                                                 |
| ------------------------------------------------ | ----------------------------------------------------------------------------------- |
| Builds to a real package                         | `npx nx build platform` → `dist/libs/platform`                                      |
| All 5 subpaths resolve through the `exports` map | `npm pack` → install in a scratch project → `require.resolve` each                  |
| Types are consumable                             | a customer-shaped `probe.ts` typechecks against the **installed** `.d.ts`           |
| A real app compiles against published types      | `npm run beta:fork` — the template, resolved to `dist/`                             |
| The surface cannot change by accident            | `npm run beta:api` — 2026 lines of signatures, watched failing                      |
| Peers are declared                               | 10 peers; every specifier in the emitted `.d.ts` is a peer or a sibling entry point |
| **`npm publish` is not refused**                 | `npm run beta:publishable` — a real `--dry-run` on a copy of the built bytes        |

### The row that was missing, and what it cost

Every row above was true when written, and the package was still **impossible to
publish**. It was compiled in Angular's _full_ compilation mode, so ng-packagr wrote a
`prepublishOnly` script into the built `package.json` whose only job is to hard-fail
`npm publish`. Phase 4 was signed off on the claim that a customer upgrade is an
`npm version` bump, and that claim had no working vehicle.

Two things are worth keeping from it:

- **§4 step 5 below would have caught this.** It says to run `npm publish --dry-run`
  before publishing. It was written and never run, because publishing was deferred — so
  the one step that exercised the failure was the one step the deferral skipped. A
  procedure step is not a check until something runs it; `beta:publishable` now does, in
  the gate and in CI, and it is the only check that executes `prepublishOnly`.
- **The cause was configuration _absent_, not wrong.** `@nx/angular:package` given a
  `tsConfig` replaces ng-packagr's bundled `conf/tsconfig.ngc.json` rather than layering
  onto it, and that bundled file is the only place `compilationMode: partial` is set.
  The identical omission had already shipped 27 wrongly non-nullable public types by
  leaving out `strict`. `libs/platform/tsconfig.lib.json` is the entire compiler
  configuration for this package, not a set of overrides.

Fixed 2026-08-24; the gate was watched failing against a deliberately reverted config
before being trusted.

## 4. What flipping the switch requires

1. Pick a scope (§1). **Human.**
2. In `libs/platform/package.json`: remove `"private": true`, add `publishConfig`.
3. If the scope changes, rename the 190 import specifiers and the `tsconfig.base.json`
   aliases, then `npm run beta:api -- --update`.
4. Decide a version. It is `0.1.0`, hand-maintained and not tied to the app version.
   A Beta wants `0.x` so the API can move without a major bump every sprint.
5. `npm publish dist/libs/platform --dry-run` to confirm the tarball, then publish.

`private: true` is what makes step 2 deliberate: `npm publish` refuses outright rather
than pushing to a scope we might not own.

## 5. Deliberately not done

- **No release CI workflow.** Publishing on a tag needs the scope decision first, and a
  workflow that publishes to the wrong registry is worse than none.
- **No provenance / signing.** Worth doing at GA; needs a public registry and a trusted
  CI publisher.
- **Version is not automated.** No changesets, no semantic-release. With one package and
  a hand-written changelog that is fine; it stops being fine with three.
- **No real `npm install` of the template against the tarball.** `fork-simulation`
  covers type resolution offline; a full install needs every Angular peer plus a
  GitHub Packages token for `@hylandsoftware/satori-ui`, so it cannot run in a fork's CI
  without secrets. The remaining uncovered risk is peer _installability_, not type
  correctness.

## References

- Package: `libs/platform/README.md`
- API snapshot: `docs/api/platform.api.md`
- Phase 4 record: `docs/phase-4-publishable-platform.md`
- Registry config: `.npmrc`
