{
  "$comment": [
    "Machine-readable mirror of AGENTS/11-beta-program.md section 4. That file is",
    "authoritative prose; this is the checkable form. `npm run beta:state` verifies",
    "every claim here against the evidence actually on disk and fails if a phase",
    "claims `complete` without a manifest that says `pass`.",
    "",
    "Rule: a phase may only be `complete` if evidence.manifest resolves to a real",
    "manifest.json whose verdict is `pass`. Anything else is `in-progress`,",
    "`blocked`, or `not-started`. Phase 1 was once recorded complete while its",
    "install path would have 404'd on every install; this file exists so that",
    "cannot be asserted without an artifact behind it.",
    "",
    "evidence.regate cites a later run against the CURRENT gate set. All three completed",
    "phases cite the SAME run, deliberately: the gate verifies the working tree against",
    "origin/main (21 affected projects), not one phase in isolation. Running it three",
    "times under three labels would be theatre, not three verifications. The original",
    "evidence.gate citation is kept because it is the historical fact of what was",
    "actually run at sign-off."
  ],
  "schemaVersion": 1,
  "evidenceRoot": "$AGENTIC_UI_EVIDENCE_DIR/beta",
  "phases": [
    {
      "id": "phase-0-unblock",
      "title": "Unblock and verify",
      "status": "complete",
      "evidence": {
        "manifest": "phase-0-baseline/latest",
        "gate": "gates/latest:phase-0-committed",
        "regate": "gates/latest:phase-3-spike"
      },
      "deviations": [],
      "notCovered": [
        "No pull request has been opened, so the PR-triggered path of every workflow is unexercised; CI has only ever run via the push trigger."
      ],
      "verifiedOnCi": "CI run 32480243192, completed success on Node 20, 2026-08-21 — the authoritative verification. Local gates run on Node 25 with a webstorage workaround and are not authoritative."
    },
    {
      "id": "phase-1-config",
      "title": "Layer 0 — upgrade-safe configuration",
      "status": "complete",
      "evidence": {
        "manifest": "phase-1-config/latest",
        "gate": "gates/latest:phase-1-remediation",
        "regate": "gates/latest:phase-3-spike"
      },
      "deviations": [
        "Declared complete once with the config path wrong by one segment; remediated after independent review. Risk R7 was dropped to Low on the strength of the wrong path and has been withdrawn."
      ],
      "notCovered": [
        "The marketplace package has never been installed on a real Nuxeo server, so R7 (upgrade-safe config survives install) stays Medium",
        "i18n extraction covers the core slice only"
      ]
    },
    {
      "id": "phase-2-registry",
      "title": "Layer 1 — extension registry",
      "status": "complete",
      "evidence": {
        "manifest": "phase-2-registry/latest",
        "gate": "gates/latest:phase-2-closure",
        "regate": "gates/latest:phase-3-spike"
      },
      "deviations": [
        "Shipped a `typecheck` target where the plan said `build`: Nx's module-boundary rule forbids a buildable library from importing a non-buildable one, and nothing in libs/ is buildable until Phase 4 adds ng-packagr."
      ],
      "notCovered": [
        "document-detail toolbar: 11 controls, 8 with dynamic state, still markup rather than descriptors",
        "the 7-item overflow menu",
        "the 5 hardcoded mat-tab children in document-detail.html",
        "route contributions",
        "multi-model adversarial review gate: never run for this phase or Phase 1",
        "browse contextMenu contributions — documentList is now live (see delivered below); contextMenu is still reserved and unread"
      ],
      "delivered": [
        "documentList slot wired end to end for browse columns: PACKAGED_BROWSE_COLUMNS registered, BrowseComponent resolves them, manifest can hide/reorder/relabel/switch-on by id. Evidence: pilot-documentlist-columns (12/12, 6 unique screenshots)."
      ]
    },
    {
      "id": "phase-3-adfhx",
      "title": "adf-hx component adoption",
      "status": "in-progress",
      "evidence": {
        "manifest": "phase-3-adf-hx/latest",
        "regate": "gates/latest:phase-3-spike"
      },
      "deviations": [],
      "notCovered": [
        "The document-list swap itself — blocked on the three API ports above, not on the component.",
        "Four capabilities to rehome from the hand-written list to the host page: error/retry, card view, thumbnails, column picker.",
        "breadcrumb, document-tree, metadata-sidebar, permissions, manage-versions, document-viewer, search",
        "bridge defects: 2 of 5 verified still open (hardcoded sys_effectivePermissions, browse_column_settings key collision)",
        "angular-oauth2-oidc and cropperjs reach the bundle and are unused by this product; pdfjs-dist tree-shakes out entirely"
      ],
      "delivered": [
        "Dependency pin set: 9 packages, all exact. adf-core declares all 16 peers as unbounded >=, so two had to be corrected against its suggestion (@mat-datetimepicker/core 16.0.1 for the Angular 20 line, pdfjs-dist 6.2.108 to avoid a native downgrade). Resolves with no --legacy-peer-deps; npm ci verified on Linux in CI.",
        "ng-mocks stubbed via tools/stubs/ng-mocks (file: dependency). adf-hx imports this test library from its shipped /ui runtime bundle; with the real library installed it reached a 1.6 MB customer chunk carrying two eval() calls. Both eliminated.",
        "bundle gate (9th): reads the built bytes and fails on test-library or eval() presence. Reports VACUOUS while adf-hx is not yet in the bundle.",
        "Before-capture of the hand-written document list: phase-3-adf-hx, 9/9 against live Nuxeo.",
        "Adoption chain measured end to end by attempting the swap: it compiles and typechecks, then fails NG0201 on upstream DocumentService."
      ],
      "blockedOn": [
        "VERSION_API_TOKEN has no implementation. Upstream DocumentService requires DOCUMENT_API_TOKEN, QUERY_API_TOKEN and VERSION_API_TOKEN, none optional, and manage-versions is a capability the POC lacks entirely.",
        "The bridge binds NuxeoDocumentApi/NuxeoQueryApi to its own clone tokens, not upstream's, so upstream DocumentService cannot see them.",
        "DocumentService and ContextMenuActionsService are not providedIn anything and need explicit providers, plus copy/move/create-version/restore-version services."
      ]
    },
    {
      "id": "phase-4-platform",
      "title": "Layer 2 — publishable platform",
      "status": "not-started",
      "evidence": null,
      "deviations": [],
      "notCovered": ["everything"]
    },
    {
      "id": "phase-5-harness",
      "title": "Layer 3 — agent harness",
      "status": "not-started",
      "evidence": null,
      "deviations": [],
      "notCovered": ["everything"]
    },
    {
      "id": "phase-6-quality",
      "title": "Beta quality bar and proof",
      "status": "not-started",
      "evidence": null,
      "deviations": [],
      "notCovered": ["everything"]
    }
  ]
}
